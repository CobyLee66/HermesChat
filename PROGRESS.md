# PROGRESS.md — 项目进度（当前状态）

> 记录跨会话的开发进度，确保新会话能快速接续。维护规则见 AGENTS.md「进度与决策文档」段。
> 项目暂无版本体系，条目以日期为锚点；已完成条目 > 25 条或本文件 > 20KB 时，压缩为一行摘要移入 `PROGRESS_ARCHIVE.md`。

---

## 当前状态（截至 2026-09-13）

### 进行中

- **「clarify 挂起时打开会话失败」修复待出包/真机复验**（2026-09-11，线上报障修复）：修复只在 JS 层（`store/chat.ts` / `rpc/types.ts` / `rpc/aggregator.ts`），**现有 09-11 APK / 桌面包不含此修复**——手机需构建机重新出包，桌面需重跑 mac/Windows 出包；复现路径：让 agent 发起 clarify（挂起 1 小时内）→ 点开该会话应正常进聊天页并显示澄清卡（旧包会弹「打开会话失败」）
- **Android「表格横向拖不动」修复已装机，待用户真机验证**（2026-09-10，D039）：根因 = 竖向 FlatList 原生触摸拦截先于子级判定、垂直位移过 touchSlop 即抢走横向拖动手势（点击后重新起手路径更平所以偶尔能拖）；修复 = 首次引入 react-native-gesture-handler 3.2.1（`GestureHandlerRootView` 挂 index.js 原生根节点 + `MarkdownText.tsx` 新 `TableScroll` 组件：Android 用 `usePanGesture` 方向锁定（activeOffsetX ±10 / failOffsetY ±8）驱动 `scrollEnabled=false` 的表格 ScrollView，惯性 JS 衰减补偿；iOS 保持原生滚动、web 分叉零改动）。tsc 0 错 / eslint 0 error / jest 294 全绿 / web:build 通过且产物零 RNGH 代码；release APK（72M，SHA256 前缀 bf3cdc64）构建机出包 + adb 装机 Success（设备 <设备序列号>）——**待用户验证**：带表格消息横向拖动应稳定生效、表格上竖滑仍滚列表、长按表格仍弹文本选择层
- **桌面端 v0.1（Windows/macOS）已实现待验收**（2026-09-06，D023~D025，`docs/desktop.md` §8）：Electron 壳 + ssh2 隧道 + 回环代理 + 三栏响应式布局 + 共享面板抽取 + 桌面能力（附件/语音/粘贴拖拽/失焦通知/自绘确认框）+ electron-builder 打包配置。mac dmg 本机出包验证通过；**待用户验收**：Electron/Windows 包真实 SSH 连接全流程、语音 webm 转写、Windows 安装包（`scripts/build-desktop-remote.sh`）
- 会话列表顶部信息 / Profile 名称显示 / 网页端气泡尺寸 / Profile 头像加载缓存打磨包（2026-09-05，1a9341a），真机效果待验证
- **会话标题即时刷新（D032）待真机确认**：mock gateway 端到端已通过（`scripts/desktop-title-refresh-smoke.js`，真实 Electron，不碰 live 数据）；手机上真机 `/title` 复测待做（逻辑三端同源，风险低）

### 最近完成
- [x] i18n 用户实测三问题修复（2026-09-14，D047 追回）— 用户 macOS（英文系统）实测暴露：①桌面 Profile 首屏右上角「退出连接」始终中文——**漏迁移的 JSX 文本**（`<Text>退出连接</Text>` 不在引号内，首版扫描器只查字符串字面量），同批挖出并修复 16+ 处同类遗漏（重连横幅×3、QQ 来源横幅×2、聊天空态、错误边界、模型「共 N 个」、回到底部、Markdown 复制菜单、重命名弹窗按钮、澄清发送、任务不存在等）；②窗口标题卡死英文「Edit profile」——ProfileEditModal 嵌套 NavigationContainer 未禁 documentTitle，劫持 document.title 后无人恢复；改为 DesktopAppShell 统一驱动标题跟随当前视图（弹层/会话/profile/视图名）+ 嵌套容器禁 documentTitle；③切换控件从 rail 底部迁出到连接主页右上角 + 桌面 Profile 首屏 header（会话/聊天视图不再提供）。`scripts/i18n-check.js` 升级为「注释剥离 + 日志豁免后行内任何 CJK 即违规」（覆盖 JSX 文本）。新增 `scripts/desktop-i18n-smoke.js`（真实 Electron + mock 网关七场景：系统跟随/持久化恢复/主页切换往返/Disconnect 随语言/首屏标题/会话视图无控件/弹层标题恢复）全 PASS；desktop-title-refresh、web-session-search 冒烟回归 PASS；tsc 0 错 / lint 0 error / jest 328 全绿。⚠ 踩坑：①**JSX 文本是硬编码文案的最大盲区**——`<Text>中文</Text>` 不是字符串字面量，只扫引号内必漏，扫描规则应为「剥注释+豁免日志后行内出现 CJK 即违规」；②**Electron 嵌套 NavigationContainer 各自驱动 document.title**，禁用主容器的 documentTitle 不代表安全，任何容器都可能写 window title，窗口标题要么显式单点驱动要么全禁；③冒烟里语言切换是三态循环（auto→zh→en），断言目标文案要点按轮询而非固定次数；④连接配置名 ≠ hermes profile 名（mock 网关返回 'mock profile'），会话视图标题断言用后者。
- [x] 界面多语言（i18n）框架 + 全量文案迁移（2026-09-13，D047，`docs/i18n.md`）— 自研轻量框架落 M6：`src/i18n/`（`locales/en.ts` 键权威源 `as const` + `zh-CN.ts` 类型强制对齐 + `t()`/`useT()` 取用 + `resolveLocale` BCP47 归一 + `deviceLocale(.web)` 系统语言检测 + `localeStore` 持久化 `hermes.locale.v1`），存量约 70 文件 400+ 条硬编码中文全量迁移为 t()；`LanguageToggle` 三态循环切换（跟随系统/中文/EN，原生挂 ProfileList header、桌面挂 ProfileRail 底部）；`scripts/i18n-check.js`（键对齐 + 硬编码 CJK 扫描 + 未使用键）入 CI 第 4 步；desktop 主进程独立翻译域 `desktop/i18n.ts`（dt() + app.getLocale，含 macOS 菜单）。验证：tsc 0 错 / lint 0 error / jest 328 全绿（新增 `__tests__/i18n.test.ts` 14 例 + `jest/setup.ts` 全局钉 zh-CN）/ `web-i18n-smoke.js` 四场景全 PASS（英文系统→英文、中文系统→中文、手动覆盖优先于系统、非法存储值忽略）+ 既有 3 个 web 冒烟回归 PASS；真机验证交构建机出包。⚠ 踩坑：①**jest 环境无原生 locale 检测**（NativeModules.I18nManager 不存在 → 初始解析回退 en），断言中文的既有测试会全挂——用 `setupFilesAfterEnv` 全局钉 zh-CN（注意不是 `setupFiles`，那里没有 beforeAll），且 setup 文件不能放 `__tests__/` 下否则被当成测试套件报「must contain at least one test」；②**跨行日志调用会骗过单行扫描**——`dlog(\n 'INFO',\n 中文模板)` 的起始行与中文行不同行，i18n-check 需要括号深度跟踪，且**日志调用检测必须先于 CJK 检测**（起始行本身无中文会提前 return）；③组件用 `const t = useT()` 后，`import {t}` 若无模块级取词点会报 no-unused-vars——只有模块级函数（alert/cronSchedule/transport 等）才需要保留 t 具名导入；④模块级文案表改键引用（label→labelKey）时必须同批改全部消费方，否则 tsc/运行时二选一炸；⑤web 端系统语言变化不热跟随、已持久化字符串（历史错误条/已存配置名）不回溯翻译均为有意取舍（见 docs/i18n.md §6）。
- [x] README 中英双语化 + 多语言路线图（2026-09-11，D046）— `README.md` 改为**英文默认展示**（面向 GitHub 国际流量），原中文内容整篇移入新建的 `README.zh-CN.md`，两版顶部互相语言切换链接；新增「Language Support / 多语言支持」章节，如实写明**界面 i18n 为计划中（未实现）**、`docs/` 设计文档暂只有中文；`docs/plan.md` 加 **M6：界面多语言（i18n）** 里程碑（先做文案抽取 `src/i18n/` + `t()` 取用层，再接语言切换与持久化，优先英文）。两版 README 章节结构一一对应（各 10 个二级标题）。另外产出可直接粘进 GitHub About 的一句话项目简介。⚠ 踩坑：①**双语文档最大的风险是结构漂移**——改一版忘改另一版，语言切换就变成欺骗用户，必须在同一次提交里改完两版（本次以「二级标题数一致 + 链接/锚点脚本校验」兜底）；②README 里的内部锚点要按 GitHub 的 slug 规则核对（`### 1. Set up Hermes Agent` → `#1-set-up-hermes-agent`，中文标题同理），改标题必须同步改锚点，否则点了不跳；③**别把「计划中的能力」写成已支持**——i18n 若写成既有功能属于虚假宣传，必须在措辞上标明「roadmap / 计划中」。
- [x] 开源配套：社区文件 + CI + lint 假基线修复（2026-09-11，D045 相关）— ①**修掉 lint 假基线**：`npm run lint` 此前报 368 errors / 34897 warnings，按目录归因后发现**全部来自构建产物**（`dist-web/` 187、`web/dist/` 180），真源码只有 1 个 error —— 根因是 **eslint 不读 `.gitignore`**；修复 = `.eslintrc.js` 的 `ignorePatterns` 显式排除 `dist*/`、`web/dist/`、`desktop/dist/`、`android/`、`ios/`、`coverage/`，并把 `CronJobForm.tsx` 里写在组件内的 `PROMPT_MIN_HEIGHT`/`PROMPT_MAX_HEIGHT` 提到模块作用域（顺带解掉 `react-hooks/exhaustive-deps`）。现在 **0 error / 45 warnings，退出码 0**，AGENTS.md 里「180 errors 基线」的说法已作废。②新增 `.github/workflows/ci.yml`（PR/push 跑 tsc + lint + test，`ELECTRON_SKIP_BINARY_DOWNLOAD=1` 省掉 100MB+ 二进制下载，带 concurrency 取消旧运行）。③新增社区文件：`SECURITY.md`（私密漏洞报告通道 + 在/不在范围内的清单 + **已知限制**：APK 用公开 debug keystore 签名、桌面包未签名、凭据本地明文）、`CONTRIBUTING.md`（trunk-based 分支规范、中文提交信息、**DCO 而非 CLA**、代码约定、敏感信息纪律、文档同步矩阵）、`.github/PULL_REQUEST_TEMPLATE.md`、`.github/ISSUE_TEMPLATE/{bug_report,feature_request}.yml` + `config.yml`（issue forms 走 YAML 表单，含「已确认不含个人信息」强制勾选）。tsc 0 错 / lint 0 error / jest 314 全绿（即 CI 三步本地跑通）。⚠ 踩坑：①**eslint 不读 `.gitignore`**——产物目录被 gitignore ≠ 被 eslint 忽略，新增产物目录必须同步加 `ignorePatterns`，否则会再次造出「几百个 error 的假基线」并让 CI 无意义地红；②排查 lint 基线别盯着总数，先按顶层目录归因（本次 368 个里 367 个是产物，真问题只有 1 个，几分钟就定位）；③issue forms 的 YAML 结构错了 GitHub 会**静默忽略整个模板**，本地用 js-yaml 校验 schema（`name`/`description`/`body[].type`/非 markdown 项必须有唯一 `id` 与 `attributes.label`）比推上去再看便宜得多；④GitHub 上 `.github/ISSUE_TEMPLATE/*.yml` 的 `labels` 若引用了不存在的标签会被静默丢弃，用默认的 `bug`/`enhancement` 最稳。
- [x] 开源前敏感信息治理 + README 重写（2026-09-11，D043）— 用 5 个并行子代理逐张审完 `docs/screenshots/` 全部 76 张截图，**21 张含真实数据**（真实 profile 名 / 会话标题 / 聊天正文 / `~/.hermes` 路径 / gateway PID / QQ 路由 key / 心理健康类会话标题），另 5 张 LOW（真实聊天暴露 provider 栈、内网 IP）。处置：①`docs/screenshots/` 整个 `git rm --cached` + 加 `.gitignore`（本地 73 个文件保留，冒烟脚本照常写入）；②本机专属值收敛到 `scripts/build-env.sh`（gitignore；模板 `scripts/build-env.example.sh` 入库），6 支构建/安装/日志脚本改为 source 它 + 通用默认值兜底（`BUILD_HOST`/`BUILD_DIR`/`BUILD_BASH`/`BUILD_ADB`/`BUILD_TMP_DIR`/`ANDROID_SERIAL`）；③文档脱敏：`/Users/<user>` → `~`、公网 IP → `<远端服务器地址>`、设备序列号 → `<设备序列号>`、统一用「构建机」指代构建主机、profile 清单改为通用描述、测试夹具昵称与真实 session id 换假值；④`AGENTS.md` 新增「敏感信息纪律（开源仓库）」段 + 收尾清单加一条自查项；⑤README 重写为面向开源的使用介绍（原 RN 模板与个人构建章节移除，改为通用四端构建说明）。tsc 0 错 / jest 314 全绿。⚠ 踩坑：①**截图是最大的隐私面**——代码与配置里 grep 不到的东西全在截图里（会话标题直接暴露职业意向、理财、心理健康、个人项目目录）；②删文件只是新增一次提交，旧 blob 仍可 `git show` 检出，要彻底清除必须 `git filter-repo --path docs/screenshots --invert-paths`；③历史里 `user`/构建机名从 root commit 起就在，只改工作树等于没改。
- [x] 「打开会话失败 iterator method is not callable」定因并修复（2026-09-11，用户报障：study profile 会话名以「搜索并下载清华 memory for LLMs」开头的会话凌晨打不开、早上又能打开）— 根因：`session.resume` 的 **`pending_clarify` 是单个对象**（源码 `tui_gateway/server.py` `_live_session_payload` → `_pending_clarify_request_payload`，`dict | None`），而客户端 `SessionResumeResult` 声明为 `ClarifyRequestPayload[]` 并在 `applyPending` 直接 `for...of` → 迭代 plain object 抛 TypeError，**Hermes 引擎的文案正是「iterator method is not callable」**（该串是 `libhermesvm.so` 内建文案，V8 是 "… is not iterable"）→ `SessionListPanel` 弹「打开会话失败」；clarify 超时（用户本机 `clarify_timeout`=3600s）后字段消失即又能打开。取证链：`agent.log`「2026-09-11 03:45:18 tool clarify completed (3600.05s)」反推挂起窗口 02:45:18–03:45:18，与「凌晨失败 / 早上正常」完全吻合；同源 bug 同样影响 `pending_approval`（也是 dict）。修复：`rpc/types.ts` 新增 `OneOrMany<T>` + 两字段改 `OneOrMany<…>`（兼容旧服务端列表形态）+ `ClarifyRequestPayload.answers`；`store/chat.ts` 新增 `asPendingList()` 在 `applyPending` **单点归一化**（flow ×2 / SshManager / fork 四个入口共用，杜绝再次误迭代）；`rpc/aggregator.ts` 的 `onClarifyRequest` 播种批量快照 `answers` → 已锁定题目回放为「已作答」（对齐官方 desktop 的 lockedAnswers）。验证：tsc 0 错 / 改动文件 eslint 0 error / jest 314 全绿（新增 `__tests__/pendingPromptsResume.test.ts` 7 例：单对象 clarify/approval、批量 answers、双挂起、列表兼容、attach/reattach 直传）；新 web 冒烟 `scripts/web-pending-clarify-smoke.js`（mock-gateway 新增 `pendingClarify` 运行期可切 + `clarify.respond` 逐题锁答案语义）**16 项断言全 PASS，且已验证旧代码必挂**（复原旧实现后冒烟报 `打开会话失败：window.alert=["打开会话失败\n… is not iterable"]`）。⚠ 踩坑：①**报错文案属于 JS 引擎、不属于项目源码**——在 src/ 与 hermes 源码里 grep「iterator method is not callable」零命中，`strings` APK 的 `libhermesvm.so` 才验明是 Hermes 内建串（V8/JSC 文案各不相同），定位第一步应按文案先分叉「前端 JS 异常 vs 服务端错误」；②服务端「挂起交互」字段（pending_approval / pending_clarify）一律按 `dict | None` 处理，无挂起时**字段直接不存在**，禁止按数组迭代；③这类「时序性打不开」先找超时/租约类日志锚点——`tool clarify completed (3600.05s)` 的 3600 直接反推出挂起窗口，比反复 UI 复现快得多；④修复只在 JS 层，**手机/桌面必须重新出包**才生效（09-11 APK 不含）。
- [x] 定时任务提示词输入区三端统一 25 行上限 + 删除桌面拖拽调高（2026-09-11，D041 修订 D040，用户要求）— `CronJobForm` 提示词 TextInput 显式 `lineHeight: 20` + `minHeight: 160`（≈8 行）/`maxHeight: 520`（25 行文本 + 上下 padding 20），随内容自动撑高、超出内部滚动；PanResponder 拖拽握把/高度 state/模块级缓存全删，两端观感一致。web 端 textarea **无原生自动长高**，沿用 ChatInputBar 先例：文本变化时先收回最小高再量宿主 textarea `scrollHeight` clamp 设高（effect 依赖 [prompt]）。tsc 0 错、eslint 0 error、jest 307 全绿；`web-cron-ui-smoke.js` 场景 3 改为「默认 160 → 填 30 行封顶 520 + 手柄已移除」，6 场景全 PASS（新截图 web-cron-ui-3-maxheight.png，520px 内恰好可见 25 行）。**09-11 release APK（72M，SHA256 前缀 de3f76df，双端一致）构建机构建并装机 Success，待用户真机复验。**⚠ 踩坑：①**RNW multiline TextInput 的 min/maxHeight 样式只是约束、不会自动长高**——必须手动量 scrollHeight 设高（原生端 EditText 才自带长高，靠样式约束即可）；②「N 行」像素 = N×行高 + paddingVertical，不设显式 lineHeight 时平台默认行高不同、上限会漂；③量高前必须先把 height 收回最小值再读 scrollHeight，否则被 clientHeight 托底删行后缩不回。
- [x] 安卓 header 视图切换控件胶囊背景拉宽修复（2026-09-11，用户真机截图报障）— 症状：安卓主页 header 里 `[会话|定时任务]` 切换控件的胶囊底色在两个按钮右侧多出一大段（拉满整个标题区）。根因：ViewSwitcher 根容器直接作 `headerTitle` 塞进原生 header 标题容器，Yoga 默认 `alignItems: stretch` 把它横向拉满，而两个分段按钮只有内容宽度；桌面端在 row 容器里（stretch 只作用纵向）所以无此问题。修复 = 手机侧（ProfileListScreen）headerTitle 包一层 `{flexDirection:'row', alignItems:'center', alignSelf:'flex-start'}` 让控件收回内容宽度——**不动 ViewSwitcher 组件本身**（桌面 profilePaneHeader 依赖 alignItems center 垂直居中，改组件默认样式会把桌面切换控件顶到上沿）。tsc 0 错、eslint 0 error、jest 307 全绿；web 渲染层无原生 header 不复现此症；**09-11 release APK（72M，SHA256 前缀 ea7a1eda，双端一致）构建机构建并装机 Success，待用户真机复验**（含本修复 + D040 五项交互）。⚠ 踩坑：①`headerTitle` 塞自定义组件必须考虑原生标题容器的 stretch 语义（可用宽度 = 整个标题区，不是内容宽）；②同一组件在手机原生 header / 桌面 row header 两种容器下的拉伸语义相反，样式修复要放使用侧而非组件侧。
- [x] 思考块展示改版：对齐工具调用卡两行结构 + 流式实时尾部刷新（2026-09-11）— 此前思考/推理块折叠头是「▸ 思考过程 + 首行前 40 字符」拼在单行里，流式期间始终钉在开头不变。改版（手机/桌面共用，仅渲染层，协议/聚合器零改动）：`ThinkingBlock.tsx` 结构对齐 `ToolCallCard`——第一行粗体标签（13/600/text）+ 右侧 chevron，第二行内容预览；容器加 hairline 描边（与工具卡一致）。第二行分态：**流式接收中**（`live`）显示尾部窗口 `thinkingTail`（最后 200 字符）+ `numberOfLines={2}` 不断刷新最新输出（dashboard 同款观感，用户选定形态）；**结束后**缩略为开头一行 `thinkingHead`（首行、120 字符截断）+ `numberOfLines={1}`；点击展开全文交互保留。live 判定收敛在 `TimelineView.isLiveThinking`：`msg.streaming && msg.blocks 末位 === 本块`（按引用比较）——依据是聚合器 `onTextDelta` 只往末位同类块追加、否则新建末位块，故「正在增长的思考块」必然是末位，工具/正文块接续或 complete 后自动收缩为开头预览。预览纯函数收敛 `utils/thinkingPreview.ts`（slice(-n) 只拷贝窗口长度，不随全文增长变慢）。tsc 0 错、改动文件 eslint 0 error（2 条 no-shadow warning 为文件既有模式）、jest 307 全绿（新增 thinkingPreview 8 例）、web:build 通过。**真机/真流式效果待用户验证**。⚠ 踩坑：①`thinking.delta` 与 `reasoning.delta` 语义完全不同——前者是服务端 busy 占位符（kaomoji），只进 `lastThinkingHint`（ChatInputBar busy 行），后者才是思考正文流（onTextDelta 进时间线块），改思考展示时别动错事件；②live 判定必须按块引用比较而非 map 索引——`showDetail=false` 时 blocks 数组被过滤重建，索引会错位。
- [x] 定时任务界面五项交互修改：点条目默认编辑 + 运行详情只读回放 + 白底居中 + 提示词可拖高（2026-09-10，D040 修订 D038）— ①任务行点击 `CronPanel` renderItem `onOpen` 从 `onOpenRuns` 改 `onEditJob`（⋯ 菜单入口不变，手机/桌面宿主已传回调零改动）；②**运行详情**：点运行记录不再 `openSessionFlow`（resume 副作用）进聊天页，改打开只读回放页——数据链 = dashboard REST `GET /api/sessions/{id}/messages`（新 `rpc/restSessions.ts`：`getSessionMessages` + `projectRunMessages` 原始行→只读投影，复用 `coerceContentText` 拍平 JSON parts；tool 行/display_kind=hidden 不渲染、压缩摘要行转 system 灰条），结构 = `panels/CronRunDetailPanel`（手机 `screens/CronRunDetailScreen` 路由 `CronRunDetail` + 桌面 `CronRunDetailModal`，desktopUiStore `cronRunDetailOpen`，详情 Modal 排运行历史之后叠上层、返回先回列表）+ 元信息卡（任务名/开始/最近活动/进行中徽章）+「在聊天中打开」（桌面先 `selectProfile` 再 `openChat`）；③④运行历史/详情弹层内容区白底（`modalPaneBody`，编辑弹层保持灰底不动）+ 720 居中（`modalPaneCenter`，对齐 cronPane）；⑤提示词输入区 minHeight 90→160（原生），web 固定高 200 + 下方拖拽握把（PanResponder 纵向调高 clamp 120–480，模块级变量运行期内记忆，accessibilityLabel「调整提示词高度」）。**桌面「点运行记录直接回列表」的根因**：CronRunsModal onOpenSession `close()+openChat()`，但聊天区只在 `selectedProfile` 非空的分支渲染（DesktopApp 壳布局），cron 首屏下永远开不出来——新详情弹层绕开该死路。jest 299 全绿（新增 runTranscript 5 例）、tsc 0 错、改动文件 eslint 0 error；新 `scripts/web-cron-ui-smoke.js`（mock-gateway 扩 cron REST `/api/cron/jobs`、`/runs`、`/api/sessions/{id}/messages` 种子）6 场景全 PASS：列表渲染→点行开编辑（名称预填）→提示词 200 拖到 300→运行历史居中（行 x=104/900 视口）→详情（元信息+气泡回放，tool/hidden 过滤断言）→两级返回，截图 `docs/screenshots/web-cron-ui-{1..5}-*.png`。⚠ 踩坑：①**RNW Modal `animationType="slide"` 动画 ~300ms，headless 截图必须先等 ≥500ms**——否则弹层只拍到动画首帧（内容缩在屏幕底部一条），断言靠 DOM 存在会误判视觉正常；②表单 ScrollView 内的拖拽手柄初版在视口外（y=1288>900），`mouse.down` 落在 html 上 PanResponder 全程不触发——冒烟先 `scrollIntoViewIfNeeded` 再拖（真机用户感知不到，滚动后可见）；③Playwright strict mode：任务行的 prompt 预览与详情气泡同文，`getByText` 撞双命中，取 `.last()`（弹层 DOM 靠后）；④`CronRunRow.started_at` 是 epoch 秒，`formatDateTime` 收 ISO 字符串（tsc 拦截），运行记录一律 `formatEpoch`；⑤冒烟自起进程清理时 `vite.kill(r)` 把回调当 signal 传 → 原始报错被 ERR_UNKNOWN_SIGNAL 掩盖且 vite 残留占口（EADDRINUSE 要先 lsof 清理）；⑥桌面详情页「在聊天中打开」先 selectProfile 后 openChat（store 的 selectProfile 会清 chat，顺序不能反）。
- [x] 定时任务（Cron）管理三端实现：主页视图切换控件 + 任务浏览/控制/运行历史/新建编辑（2026-09-10，D038）— 主页（ProfileList）左上角「Hermes」标题位换成视图切换控件 `[会话 | 定时任务]`（`ViewSwitcher`，默认会话、原 profile 列表不动），右上角退出保留；定时任务视图与 dashboard cron 管理对齐：任务列表（名称 + 状态徽章 已调度绿/已暂停黄/出错红/已完成灰 + profile 徽章 + 计划中文人性化描述（每周一、三 14:30 等）+ repeat + 下次/上次运行 + 错误明细行（漏触发>投递失败>运行错误）+ prompt 预览）+ profile 筛选下拉 + 行 ⋯ 菜单（暂停/恢复、立即运行(行内转圈,409 已在运行特判)、编辑、运行历史、删除 confirmDialog）+ 下拉刷新 + `cron.changed` 事件自动刷新（防抖 1s 静默）；运行历史页 = 该任务最近 20 条运行会话（`/runs`），点记录 `openSessionFlow` 进聊天页查看；新建/编辑表单 = 名称/提示词/六模式计划构建器（间隔/每天/每周/每月/一次性/自定义表达式）/投递目标（delivery-targets，未配置 home 渠道灰字提示）/归属 profile（仅新建）/跟随上次输出（context_from ['self']）。协议选型 D038：dashboard REST `/api/cron/*`（9119 同进程、复用会话 token；`rpc/cron.ts` 纯函数封装，trigger 单独 10 分钟超时）而非 WS cron.manage（无 update/trigger/runs）或 8642 `/api/jobs`（第二凭据）。结构：`rpc/types.ts` Cron 类型 + `rpc/cron.ts` + `store/cron.ts`（cron.changed 按 RPC 实例自接线，放 store 内避免 connection↔cron 循环导入）+ `utils/cronSchedule.ts`（构建/回显反解/中文描述纯函数）+ `components/ViewSwitcher.tsx` + `panels/CronPanel|CronRunsPanel|CronJobForm`（手机/桌面共用）+ 手机路由 CronRuns/CronEdit + 桌面壳首屏 ViewSwitcher 切换 + CronEdit/CronRuns Modal（desktopUiStore cronEditOpen/cronRunsOpen/homeView）。jest 294 全绿（新增 cronSchedule 14 + cronStore 7）、tsc 0 错、改动文件 eslint 0 error；web 冒烟（vite 代理真连 live 9119，全程只读）：连接→视图切换→列表（14 任务徽章/中文计划/错误行全对）→行菜单→运行历史（4 条记录渲染）→新建表单→模式选择卡→周 chips→切回会话，全 PASS，截图 /tmp/smoke_{1..7}*.png。**三端构建 + Windows 真机测试（2026-09-10）**：release APK 70M（`dist/app-release.apk`，构建机构建，手机未连按预期跳过安装）、mac dmg 159M（`dist-desktop/HermesChat-0.1.0-arm64.dmg` + mac-arm64/.app，`CSC_IDENTITY_AUTO_DISCOVERY=false` 免签名）、Windows Setup exe 264M（`dist-desktop/HermesChat Setup 0.1.0.exe`，构建机 electron-builder）；**Windows 端真机冒烟 PASS**——构建机上启动 win-unpacked HermesChat.exe（SSH 经 PowerShell 直启；`start` 引号嵌套在 ssh+cmd 下不生效，改 Start-Process 亦静默失败，最终前台直启成功、进程在 Services 隐藏会话运行不影响功能）+ `--remote-debugging-port=9222`，Mac 侧 `ssh -L 9222` 隧道 + playwright-core `connectOverCDP` 驱动真实渲染层：`ssh -R 9119` 反向隧道把 Mac gateway 暴露给构建机的 127.0.0.1:9119（health 200），注入 direct 测试 profile（保留原 localStorage，测后恢复）→ 自动直连 → 切定时任务 → 列表 14 任务徽章/中文计划/错误行全对 → 行菜单五项 → 运行历史（4 条渲染）→ 新建表单字段齐全，console 零错误，截图 /tmp/win_{1..5}*.png，测后应用已杀、隧道已拆、临时脚本已删。⚠ 踩坑：①**jest.mock 工厂引用外层变量会被 babel-jest-hoist 静默绑成 undefined**（`const fakeRpc={on:mockOn}`+工厂引用 fakeRpc → 运行时 fakeRpc.on 变 undefined，不报错只坏数据，最小复现 8 个 probe 才定位）——工厂必须自包含（fn 在工厂内创建、挂 `__mocks` 从 requireMock 取回）；②**jest.resetAllMocks 会把 mock 模块顶层导出的函数属性清成 undefined**（同一 fn 对象挂在嵌套 __mocks 下则无恙、store 的 import 绑定也不受影响）——mock 实例一律经 `__mocks` 引用；③测试 flush 用 `setTimeout(0)` 在 fake timers 下永不触发直接超时——改纯微任务排空（`for await Promise.resolve()`）；④9119 是 dashboard 进程内嵌 tui_gateway（`/api/ws` 直接调 `tui_gateway.ws.handle_ws`），`/api/cron/*` 与 WS 同 token 同端口——「dashboard REST 只有 cookie 认证」的直觉是错的，`X-Hermes-Session-Token` 全部放行（web_server.py:1099 中间件）；⑤web 冒烟注入 localStorage `hermes.connections.v2`（direct + host 'web-direct' 哨兵 + autoProfileId）即可全自动真连 live gateway，配合 playwright-core + 系统 Chrome（本机无 python playwright）零成本搭三端验证环境；⑥console 里 `/favicon.ico` 404 是项目历史缺口（index.html 无 icon），与功能无关；⑦**Windows OpenSSH 起 GUI**：`start`/`Start-Process` 在 ssh+cmd 双层引号下各种静默失败，最稳的是 ssh 直启 exe 本体（前台阻塞，套 run_in_background）；CDP 驱动 Windows 真实 Electron = `--remote-debugging-port` + Mac `ssh -L` 转发 + playwright-core `connectOverCDP`，配合 `ssh -R` 反向隧道喂 gateway，即可在本机全流程验收 Windows 包，无需人坐在构建机前。
- [x] 聊天记录查找 + 会话列表搜索 + 筛选下拉底色调淡（2026-09-10）— ①会话右上角 ⋯ 菜单加「查找聊天记录」（ChatOverlays 新 prop `onOpenSearch`，foreign 只读会话同样可用）：顶部展开搜索栏（`src/components/SearchBar.tsx`，聊天与会话列表共用），输入即搜已加载消息并自动跳第一处，计数 n/m + `↑`/`↓` 循环跳转 + `✕` 关闭清状态；匹配纯前端（历史全量在内存）收敛在 `src/utils/timelineSearch.ts`——只搜可见对话文本（user text / assistant text+error 块 / system 灰条），thinking/reasoning/tool 卡不搜（默认可能隐藏，跳过去看不到匹配）；滚动定位 = TimelineView handle 新增 `scrollToMessage(id)`（inverted 索引换算 + `viewPosition:0.5` 瞬时定位 + `followRef=false` 防流式钉底拉回 + `onScrollToIndexFailed` 估算偏移/限次重试兜底），当前命中消息整体 accent 描边（`highlightMessageId` prop 包最外层，不侵入两套 MarkdownText）；手机 Android 返回键先关搜索栏、桌面 Esc 链最前优先关搜索。②会话列表（SessionListPanel，手机/桌面共用）：工具行排序药丸右侧加「搜索」文本药丸按钮（‹/⋯/▾ 文本字符惯例，不加图标资产），展开共用 SearchBar（无导航区版），`src/utils/sessionSearch.ts` 按标题（空标题按「未命名会话」参与匹配）+摘要不区分大小写过滤，搜索栏与双下拉菜单互斥展开、BackHandler 先收搜索/菜单、空态文案区分「该分类下暂无会话」vs「未找到匹配的会话」。③筛选/排序药丸底色 `Colors.fill`(#EDF0F5) → 新 token `fillSubtle`(#F5F6F8)（白底上略深一点可辨轮廓即可；不动全局 fill 免波及 ChatInputBar 输入区圆底）。jest 273 全绿（新增 timelineSearch 7 例 + sessionSearch 5 例）、tsc 0 错、改动文件 eslint 0 error、web:build 通过；Playwright 冒烟双脚本全 PASS（`scripts/web-chat-search-smoke.js`：菜单→搜索栏→计数 1/30→自动跳最早命中+高亮→↓×10 中段位移→↑×10 回首处→✕ 清高亮；`scripts/web-session-search-smoke.js`：搜索栏展开→命中保留/无命中空态→与下拉菜单双向互斥→关闭恢复，端口 9201/5189、9202/5190 环境变量可换道），截图 `docs/screenshots/web-{chat,session}-search-*.png`。⚠ 踩坑：①RNW `onScrollToIndexFailed` info 字段是 `averageItemLength`（不是 averageFrameLength）；②RNW `animated:true` scrollTo 是 JS 驱动动画，**布局刚变（搜索栏插入压缩视口）后立即触发会静默不滚**——查找跳转改 `animated:false` 瞬时定位（微信同款），冒烟 fill 前留 400ms 重排；③inverted 列表最早几条命中贴内容顶部，`viewPosition:0.5` 被 maxScroll clamp、相邻命中位移不可分辨——断言跳转位移要连跳到中段再比；④桌面两栏壳下会话列表的菜单 backdrop 只盖左列不是整窗，`mouse.click` 收菜单坐标必须在列内；菜单开着点工具栏按钮首击只会「点外收起」（既有交互），脚本要分两击；⑤并行会话把 PROGRESS/文件同步推新（TimelineView 在编辑间隙被 D037 滚动修复更新过）——编辑前重读、撞「file has been modified」就在最新版上叠加。
- [x] 聊天「首次上滚卡顿/抖动」修复：锚定补偿收窄至流式增长 + web 首滚渲染减负（2026-09-09，D037 修订 D035）— 症状「刚进长会话首次上滑时滚动抖动（像内容高度变化导致滚动位置跳），浏览一遍后前后滚动就平滑」。定因三层：①**锚定补偿对非流式增长误触发（主因）**——历史进会话全量入列但 FlatList 只渲染窗口内 cell，首次上滚旧 cell 逐批懒挂载使 contentSize 阶梯增长；D035 的 handleContentGrowth 把一切非跟随增长都当流式在底部生长、前移 offset「补偿」，而挂载增长在视觉顶端一侧本不移动视口，补偿反而把用户向前瞬移 deltaH 并打断惯性滚动（原生分支 `offsetUntouched(lastOffsetRef.current, atGrowth)` 是同值自比较**恒真**＝每次增长必跳；web 靠下一帧读真实 scrollTop 大多能跳过，但滚轮档间停顿时命中）。已挂载 cell 在 windowSize（默认 21 视口）内不卸载，故二次滚动零挂载零增长自然平滑；②web 每气泡插一份重复 `<style>`（MarkdownText.web）批量挂载时反复全文档样式重算，且每气泡同步 markdown-it 解析；③markdown 图片无尺寸（原生 FitImage 初始高 0 异步回填 / web img 解码前高 0）逐张撑高。修复：`shouldRestoreAnchor` 收窄为「流式增长 + 用户停滚」（距上次 scroll 事件 <200ms 视为滚动中，滚动中不补——顺带覆盖「流式中上滑滚入未挂载区」这一增长来源不可分辨的场景，旧代码此处照样前跳，被新冒烟场景 2 实测暴露）；TimelineView 读 store `busy` + `lastScrollAtRef` 门控；web `<style>` 模块级单次注入 + md.render 结果 FIFO 缓存（300 条）；FlatList `maxToRenderPerBatch` 10→6（windowSize 保持默认）。jest 261 全绿（timelineFollow 用例扩展 streaming/scrolling 参数）、tsc 0 错、改动文件 eslint 0 error；`scripts/web-scroll-follow-smoke.js` 扩到 5 场景全 PASS，新场景 0（mock `historyCount:200` 进会话分 10 档上滚，断言 scrollTop ≤ 累计滚轮量+60 且 scrollHeight 确有增长——懒挂载路径真实命中，旧逻辑必挂），截图 `docs/screenshots/web-scroll-follow-{0..4}-*.png`。⚠ 踩坑：①「补偿以视口未被移动为前提」在原生端是空话——atGrowth 与比较值同源自 lastOffsetRef，自比较恒真；写「守卫」先审比较对象是否可能不同；②windowSize=21 视口的初始渲染窗 ≈9900px，冒烟要稳定命中懒挂载，种子历史必须高过它（80 条 ≈6400px 全在窗内、进会话后异步填满，两次运行结果随机——断言依赖懒挂载时必须先算渲染窗容量）；③动画 scrollToOffset（回底按钮）长距离 500ms 等不稳，冒烟断言要轮询；④5188/9199 端口偶被并行会话占用（本次另一 zcode 会话起了 vite dev 5188），冒烟端口改环境变量可换道（`SMOKE_VITE_PORT`/`SMOKE_MOCK_PORT`）；⑤流式期间 items 高频变化会让 render 缓存流水污染（流式中间文本进缓存命中不了），FIFO 上限兜内存即可，不做精细失效。

- [x] 头像「深色圆角线框」修复：Avatar 单层圆角化 + 去垫底色 + ImageResizer 白底展平补丁（2026-09-09 定因，2026-09-10 桌面双端验证通过，D036）— 用户报「头像四角圆角边缘有深色线框，重传原图复现」疑为前端裁剪瑕疵。像素级定因：①用户原图 + 7 个存量 avatar.jpg 边缘灰度剖面全部干净（223~255，暗值是头发/衣服内容）→ 裁剪无罪；②最小复现实验室（vite dev 临时页 `web/avatar-lab.{html,jsx}` import 真 Avatar + 结构变体 × 纯白 JPEG，Playwright DPR3 + ffmpeg 像素环分析）纯渲染层无框 → 排除结构性 bug；③真实文件 96px 渲染放大 + **色度（RGB 饱和度）环分析**实锤根因：`Avatar` 给 Image 垫的 avatarColor 底色从圆角裁剪的抗锯齿边缘渗出，白底头像四周形成一圈彩色「瑕疵线框」（灰度分析/小尺寸目视都会漏，必须查 RGB 色度）；外层 View+内层 Image 双层同半径 borderRadius 为同类加重因素。修复：`Avatar.tsx` 圆角只保留一层（Image 自身）且**图片分支彻底去掉垫底色**（无图回退分支保持色块），三端无需重传头像即生效；顺带引入 **patch-package**（devDep + `postinstall`）打 ImageResizer JPEG 白底展平补丁（Android `saveImage` + iOS `saveImage`，根治「透明 PNG 转 JPEG 烤黑边」隐患——预乘 alpha 透明处 RGB=黑，本次原图不透明未触发），补丁 `patches/@bam.tech+react-native-image-resizer+3.0.11.patch`。tsc 0 错、Avatar.tsx eslint 0 error、jest 261 全绿；**2026-09-10 用户桌面双端（Mac dmg / Windows exe）验证通过**，无需重传头像。⚠ 踩坑：①小尺寸截图目视判读会骗人——46px 头像「深色线框」实为深色头发贴边的错觉（我据此误判 web 也有框，逐像素 RGB 剖面才翻案），定因必须像素取证（ffmpeg rawvideo gray/rgb24 → node 统计），彩色瑕疵必须查色度不查灰度；②尺寸换算先行：显示端 1px 线框 ⇔ 512px 源文件需 ~11px 暗带，换算不上就别怀疑文件；③ZCode plan 模式沙箱拦截一切像素工具（python/node/ffmpeg/sips 落盘全禁），定因实验放实施阶段第一步做；④RN 圆角只允许一层，且**圆角元素下方不要垫彩色底**——抗锯齿边缘必然混入底色（RNW/Android 真实 GPU 窗口可见，无头截图复现不出，平台差异要分别取证）；⑤RNW 0.21 Image = background-image div + 外层 overflow hidden，结构变体矩阵是隔离渲染层肇因的最快手段，但 GPU 合成类瑕疵需有头/真实窗口复现；⑥patch-package 首次引入：npm ci 会跑项目 postinstall（构建机构建链兼容），补丁只含 Java/ObjC 不影响 jest；⑦`~/.hermes/profiles/<name>/avatars/` 是 hermes 自己的 agent 头像产物，与 App 的 assets/avatar.jpg 无关，排查别混。
- [x] 「打开会话失败 rpc not connected」偶发修复（2026-09-09）— 症状「点会话偶发报 rpc not connected、再点就好」：断线瞬间 `handleDrop` 同步 `setRpc(null)` 进 reconnecting（1s 退避起步，回前台探活判死/WS 被后台掐断是高发触发），点击撞进这个 1~2s 窗口 → `openSessionFlow → sessions.resume → getRpc()`（runtime.ts:16）同步抛错。修复 = flow 层（手机/桌面共用 `src/panels/sessionFlows.ts`）三处入口 `await useConnectionStore.getState().waitReady()`（openSessionFlow / createSessionFlow 开头 + deleteSessionFlow 确认框之后）——waitReady 在 reconnecting 会先 retryNow 跳过退避加速重连再轮询等就绪（15s 超时报「连接恢复超时」），disconnected 报「未连接」；不改回前台探活单次判死（D028 有意决策）。jest 261 全绿（新增 sessionFlowsOpen.test.ts 3 例：ready 直通 / disconnected 不发 RPC / reconnecting 恢复后成功打开）、tsc 0 错、改动文件 eslint 0 error（sessionUsageSync.test.ts 补 `useConnectionStore.setState({state:'ready'})` 种子适配）。⚠ 踩坑：①**这是 waitReady 先例第二次被抓**——ProfileEditScreen.tsx 选图场景已用同款注释修过一模一样的「偶发 rpc not connected」，会话 flow 层又漏了：**任何用户交互入口发 RPC 前必须先 `waitReady()`**，新增 RPC 调用点时对照检查（现有覆盖：ProfileEdit 保存/选图、会话 open/create/delete）；②「非必现 + 再点就好 + rpc not connected」这组指纹直接指向断线重连窗口（setRpc(null) → 1s 退避），不用怀疑 RPC 本身；③flow 层测试里 connection store 默认 state='disconnected'，直接调 flow 的既有测试会被 waitReady 拦截报「未连接」——beforeEach 种 `setState({state:'ready'})` + runtime mock `hasRpc:()=>true` 即直通。
- [x] 三端构建发布包（2026-09-09）— e08d64d（6 功能单元）全量验证后出三端包：tsc 0 错 / eslint 0 error / jest 258 全绿；冒烟复跑全 PASS（desktop-usage-sync 4、desktop-title-refresh 4、web-scroll-follow 4、web-session-sort 28 行两档、web-profile-validation 5 组）；mac dmg（159M）与 Windows `HermesChat Setup 0.1.0.exe`（264M，构建机 `build-desktop-remote.sh`）取回 `dist-desktop/`；release APK（70M）构建机构建 + adb 装机 Success（设备 <设备序列号>）+ scp 取回 `dist/app-release.apk`（SHA256 b58ebadf 与远端一致）。⚠ 踩坑：①**ZCode agent 环境跑 `build-macos.sh` codesign 必报 errSecInternalComponent**——agent 的 bash 跑在 `launchctl managername=Background`（非 GUI）上下文，无法访问 login keychain 私钥（`security find-identity` 能列证书但签不了名），手动 codesign 同文件可复现；agent 出包用 `CSC_IDENTITY_AUTO_DISCOVERY=false bash scripts/build-macos.sh` 出未签名 dmg（Gatekeeper 拦截时右键→打开），签名版需用户在 GUI 终端重跑脚本；②`web-session-sort-smoke.js`/`web-profile-validation-smoke.js` 前置是**外部 vite dev server**（localhost:5188，直接跑报 ERR_CONNECTION_REFUSED），与自起 build+preview 的 web-scroll-follow-smoke 不同——跑冒烟前看脚本头部「前置」注释；③build-android-remote.sh 卡 ssh 不退出（09-05 已记）本次复现，kill 后手动 scp + certutil/shasum -a 256 双端校验即可。

- [x] 聊天流式输出滚动跟随：上滑暂停 / 回底恢复 / 发送回底（2026-09-09，D035）— 此前「贴底跟随」全靠 inverted FlatList 隐式行为，用户上滑阅读历史时流式内容增长把视口往新消息方向拖（Android RN ScrollView 无可见位置锚定；web Chrome anchoring 不可依赖）。实现收敛在 TimelineView（手机/桌面共用）：`src/utils/timelineFollow.ts` 纯函数（距底 ≤32px=在底部）+ `followRef`（onScroll 同步更新，sessionId 变化重置）+ onContentSizeChange 增长时跟随态显式 `scrollToOffset(0)` 钉底、非跟随态锚定补偿（原生直接 scrollTo(offset+delta)；web 下一帧经 `getScrollableNode()` 读真实 scrollTop，仅未被调整过才补，避免与 Chrome anchoring 双重补偿）；「↓ 回到底部」点击显式置跟随；新增 `revealBottom()` ref 句柄 + useChatComposer 可选 `onAfterSend`（裸 /model 早退不触发），上滑状态下发消息立即回底并跟随。冒烟基建：mock-gateway 扩展 `prompt.submit` 流式回包（message.start→delta×N 定时→complete，事件帧带 session_id 路由）、vite 上游可 `HERMES_VITE_UPSTREAM` 环境变量覆盖、新 `scripts/web-scroll-follow-smoke.js`（自起 build+preview+mock，全程不碰 live 9119）四场景 PASS：贴底跟随钉底 / 上滑暂停阅读位置稳定（scrollTop 272→620 由 Chrome anchoring 自动补偿，实测其在位）/ 回到底部恢复跟随 / 发送回底跟随新 turn，截图 `docs/screenshots/web-scroll-follow-{1..4}-*.png`。jest 258 全绿（新增 timelineFollow 4 例）、tsc 0 错、改动文件 eslint 0 error。⚠ 踩坑：①Playwright 断言「DOM 存在」≠「视口内可见」——上滑暂停时新段渲染在视口下方（视觉底部屏幕外）是**正确行为**，用可见性断言流推进会假超时（本次最大弯路：mock deltaSent=26 全到达却报「流断了」，实为断言语义错）；②web 端补偿必须下一帧读滚动节点真实 scrollTop——onScroll 有 32ms 节流，用节流状态判断 anchoring 是否已调整会竞态双重补偿；③vite dev server 首次打开聊天界面会发现新依赖（markdown-it 等）触发重新预构建 + **整页 reload**（App 被重启回首页），进聊天界面的冒烟必须 build+preview（preview 复用 server.proxy 配置）；④`page.evaluate` 回调序列化后引用不了 Node 侧常量（闭包断裂 ReferenceError），外部值必须作参数传入；⑤RNW VirtualizedList 对 inverted 列表有专门 wheel 方向补丁（光标在 cell 上时 `node.scrollTop -= deltaY`），真实滚轮方向自然，冒烟可直接 mouse.wheel；⑥mock-gateway / PROGRESS / DECISIONS 存在并行会话同步骤新的可能，编辑前必须重读（本次 mock-gateway 中途被并行会话加了 session.usage，在最新版上叠加）。

- [x] 重进会话主动同步上下文信息（2026-09-09）— 痛点：重进会话后顶栏「模型 · 上下文用量 · 思考等级」要等新消息输出才刷新。根因：顶栏信息 = chat store `info`，更新源只有 resume/create 种子（**普遍缺 usage**——计数器是 gateway 进程内状态，docs/protocol.md §3）、turn 事件（session.usage 每秒 / message.complete）、改名/压缩/切模型广播；空闲会话重进只有种子且种子缺 usage。修复：对齐 `refreshTitle`「主动读回」先例——chat store 新增 `syncSessionInfo(sid)` 调 `session.usage` 只读 RPC（**顶层 usage dict**，含 model + context_used/max，与事件同源；只认 live sid → 必须 resume 后调），合并 usage 并顺带校正 model；调用点两处：`openSessionFlow` 正常/fork 分支（attach 后 fire-and-forget，不阻塞导航）+ `SshManager.resumeActiveSessions`（重连恢复，顺带修「重连后 info 完全不刷新」——reattachAfterResume 现落地 resume 的 info）。让位规则：streaming 中不落地（ticker 每秒会推）。jest 258 全绿（新增 `__tests__/sessionUsageSync.test.ts` 8 例）、tsc 0 错、改动文件 eslint 0 error；Electron 冒烟 4 PASS（`scripts/desktop-usage-sync-smoke.js`，mock gateway 补 session.usage 并保真「resume info 无 usage」：首开会话顶栏 ≤3s 出现 3.5k/8k 未发任何消息、resume 后必调 session.usage(live sid)、发消息后 complete 事件更新 100/8k、重进顶栏再变回 3.5k/8k，截图 `docs/screenshots/desktop-usage-sync-{1,2,3}-*.png`）。⚠ 踩坑：①`session.usage` RPC 返回**顶层 dict** 不是 `{usage:…}`（事件 payload 才包 usage 键）；②agent 未建时返回零计数 `{calls,input,output,total}`——无 model 无 context_*，落地后顶栏用量段自然不显示，别当 bug；③冷路径（会话被 gateway 回收后重进）usage 从 0 重计，主动读回也拿不到真实用量，属服务端口径（远端服务器 hermes 版本旧，session.usage 行为需真机复测）；④sync 放 sessionFlows/SshManager 调用层而非 attach 内——attach 被大量单测直接调，放 store 内会让 mock RPC 串台（toHaveBeenCalledTimes 断言炸）；⑤mock gateway 的 9199 端口偶发残留监听进程（EADDRINUSE），lsof 确认进程已死后重跑即可。

- [x] 连接配置编辑页：placeholder 灰色 + 保存必填校验（2026-09-08）— ①该页 8 个 TextInput 均未设 `placeholderTextColor`（走平台默认深色，与正文 #1A1A1A 难区分），全部补 `Colors.textSecondary` 灰（对齐 ProfileEditScreen 等既有口径），顺带给「用户名」「私钥口令」补占位示例；②新增 `src/utils/profileValidation.ts` 纯函数：主机必填（SSH/Gateway 文案区分）、端口 1–65535 整数、SSH 类型用户名必填 + 密码/私钥至少一项，直连不查用户名/凭据（token 选填）；保存时 `alertError('无法保存', 全部错误)`（跨端弹窗）+ 出错字段红边框（`Colors.danger`）+ 字段下方红字，重新输入即时清错、切连接类型清空错误。jest 246 全绿（新增 profileValidation 9 例）、tsc 0 错、改动文件 eslint 0 error、`web:build` 通过；Playwright 冒烟 `scripts/web-profile-validation-smoke.js` 5 组断言全 PASS（placeholder 计算色=rgb(138,143,153)、清空端口后空表单保存弹 4 条错、红框红字、输入即清错、直连无凭据可保存），截图 `docs/screenshots/web-profile-validation-{1,2}-*.png`。⚠ 踩坑：①端口默认预填 '22'/'9119'（EMPTY_PROFILE），空表单校验只报 3 条错、「请填写端口」仅在被清空时触发——测默认值表单别想当然数错误条数；②连接首页卡片名称与地址行（root@10.0.0.9:22）都含主机文本，Playwright `getByText(主机名)` 必须 `{exact: true}` 否则 strict mode violation；③早期页面容易漏 `placeholderTextColor`（RNW 默认 placeholder 色偏深），新页面一律显式设置。

- [x] 语音「中文识别成英文」根因定位 + 服务端配置修复（2026-09-08，D034 修订 D020）— 根因：`/api/audio/transcribe` 的 `?profile=` 经 `_config_profile_scope` 把配置作用域切到**会话所属 profile 的 home**，STT 语言/模型按该 profile 自己的 config.yaml 解析；用户 09-06 已把 main/default 改为 `stt.language: zh`（且 load_config 按 mtime 热生效无疑），但 finance/study/work/mental-health 四个 profile 仍缺 → 语言=None 自动检测 → faster-whisper `base` 误判 `lang=en`（09-08 21:42 日志 `Transcribed hermes-desktop-voice-*.m4a ... lang=en, 22.3s audio` 实锤，`hermes-desktop-voice-` 前缀即该端点临时文件 web_server.py:5381，时间线与 App 语音操作吻合）。经用户**明确授权**把本机 6 个 profile 全量配 `language: zh` + `model: small`（仅 6 处 yaml，未重启进程、未碰服务端源码与 App 代码）；「换前端本地 STT/免费云 STT」经评估否决（Electron Web Speech 无 Google key 不可用、Android 需新原生模块、语音出本机+配额风险，云识别应走服务端 stt provider）。协议结论已记 `docs/protocol.md` §附件。⚠ 踩坑：①排查「配置改了没生效」先问**这段代码在哪个作用域读配置**——multiplex 下每个 profile 一份 config.yaml，改两处≠全局生效；②`load_config()` 缓存按 (mtime_ns, size) 逐次校验、按路径分键，改 yaml 即热生效且 profile 间不串缓存，无需重启进程；③`hermes-desktop-voice-*` 临时文件名是定位 transcribe 请求来源的路标。

- [x] 会话列表两档排序：「最近消息」（默认）/「创建时间」（2026-09-08，D033）— 过滤条下新增排序分段条（手机/桌面共用 SessionListPanel），档位持久化 AsyncStorage `hermes.sessionSort.v1`（App 启动恢复）。「最近消息」= 保持服务端 `session.list` 返回序（源码确认写死 `order_by_last_active=True`，投影无 last_active 值但顺序即权威、压缩链感知）——此前 `projectProfileSessions` 按 started_at 重排把该顺序破坏掉了（旧列表实为创建时间序）；multiplex 跨库合并（有 foreign 行）时按 namespaceMap exec 扫描的活跃时间交错（扫描 SQL 从 `agent:%` 过滤放宽为全表三字段 `id\tkey\t活跃时间`，活跃 = `MAX(last_activity_at, MAX(messages.timestamp))` 兜底 started_at，同服务端 `_sql_session_last_active` 口径；真实 main 库副本 336 行实测 35ms，本地 /tmp 副本验证不碰 live）。「创建时间」= 纯客户端 started_at 降序（`src/utils/sessionSort.ts`）；最近消息档行时间展示活跃时间（web 直连无 exec 时回退创建时间）。jest 237 全绿（multiplex 更新 + sessionSort 新增 11 例）；web 直连 live 9119 只读冒烟通过（`scripts/web-session-sort-smoke.js`：期望序由只读 RPC 同口径计算，28 行两档行序断言 + localStorage 持久化断言，截图 web-session-sort-1/2）。⚠ 踩坑：①服务端「有序无值」——排序千万别用 started_at 近似 last_active（压缩链/活跃会话场景两者差整个量级）；②单库列表（无 foreign）不得用扫描值重排，服务端压缩链感知序 > 行级近似；③异步偏好恢复放 App.tsx 启动 effect（放面板挂载会闪一帧默认档）；④RNW 行定位按唯一标题文本 + boundingBox y 排序最稳，重名/空标题行必须从断言集剔除。
  - **UI 改版（2026-09-09）**：过滤/排序双行分段改**单行双下拉**（用户反馈双行丑且不对齐）——工具栏一行 space-between，「聊天 ▾」靠左「最近消息 ▾」靠右，药丸当前值 + `▾` 字符（‹/⋯ 文本字符惯例，不加图标资产）；点药丸弹 absolute 菜单卡（SlashSuggest 浮层先例，非 Modal——免 Android Modal 坐标偏移/返回键问题），`left:12`/`right:12` 与工具栏边距对齐零坐标换算，`top` 取工具栏 onLayout 实测底边（免疫字体缩放），`StyleSheet.absoluteFill` backdrop 点外收起；Android `BackHandler` 菜单开时先关菜单。数据链路/排序逻辑零改动。冒烟适配（点药丸→菜单选选项，420px 窄档截图 web-session-sort-3）通过。


### 待办 / 下一步

- [ ] 「clarify 挂起时打开会话」修复出包 + 真机复验（2026-09-11）：构建机出 release APK（当前 09-11 包不含此修复）；真机复现路径 = 让 study/main 任一 profile 的 agent 发起 clarify → 挂起期内点开会话（旧包弹「打开会话失败 iterator method is not callable」，新包应正常进聊天页并显示澄清卡、可点选作答）；桌面包（mac dmg / Windows Setup exe）同样需重出
- [ ] 定时任务视图手机真机验证（2026-09-10，D038+D040）：**09-11 最新 APK 已装机 Success**（设备 <设备序列号>，72M，SHA256 前缀 ea7a1eda 双端一致，含 D040 + 思考块改版 + header 切换控件拉宽修复）——顺带看：header `[会话|定时任务]` 胶囊应贴住两个按钮、右侧不再多出底色——原生 headerTitle 的切换控件、定时任务列表/筛选/下拉刷新、行菜单暂停/恢复；**D040 新交互**：点任务行应进编辑（不再进运行历史）、运行历史点记录进运行详情页（白底、元信息卡 + 只读对话回放、可「在聊天中打开」）、编辑页提示词默认 ≈160 高（原生无拖拽手柄）
- [ ] 思考块改版真机验证（2026-09-11）：聊天中 agent 思考时应显示粗体「思考过程」标签 + 第二行最新两行实时滚动（不再钉在首行），思考结束自动缩为首行开头预览，点击展开全文；同包已验证 Windows Setup exe 重建取回（`dist-desktop/HermesChat Setup 0.1.0.exe` 368M，SHA256 前缀 cbc17ac4e5143c80 双端一致，09-10 Windows CDP 冒烟流程可复用），桌面/web 与手机同渲染层
- [ ] 定时任务写操作实测（2026-09-10，D038）：暂停/恢复、立即运行（注意是同步长操作）、删除、新建/编辑保存——**均属对 live 服务的写操作，逐项征得用户批准后再验**（AGENTS.md 纪律）；Windows/web 冒烟均只做了只读浏览
- [ ] 聊天记录查找 / 会话列表搜索真机验证（2026-09-10）：Android 真机——⋯菜单「查找聊天记录」→ 输入关键词应跳转+高亮、↑↓ 循环、✕ 关闭、硬件返回键先关搜索栏；会话列表「搜索」药丸展开过滤、与下拉互斥、返回键先收；跳转定位主要看原生 ScrollView 无 anchoring 时 scrollToIndex（onScrollToIndexFailed 兜底路径）实测表现；构建机出包后顺带验证
- [ ] 头像修复 Android 真机验证（2026-09-10，D036）：新 release APK 已出并装机 Success（设备 <设备序列号>，含单层圆角化 + 去垫底色 + 白底展平补丁；APK 取回 `dist/app-release.apk` 70M，SHA256 前缀 f4c793d838168c02 双端一致）——**待用户在手机上看线框是否消失**；顺带可选验白底展平：用带透明的 PNG 换头像，透明区应烤成白底而非黑边。（注：`build-android-remote.sh` 末尾 ssh 挂起问题本次复现，kill 后手动 scp + 双端校验兜底）
- [ ] 滚动跟随真机/桌面复测（2026-09-09，D035+D037）：Android 真机（流式输出中**停滚后**上滑阅读应停在原位不被拖走、滚动过程中视口只随手势动、滚回底部恢复跟随、上滑时发消息立即回底；D037 已把补偿收窄为「流式+停滚」，原生端主要看停滚后流式增长的补偿是否稳、以及慢速滚动中流式增长不再瞬移）+ Electron 桌面（与 web 同渲染层，冒烟已覆盖，抽查即可）；构建机出包后顺带验证；若真机仍见上滚抖动，下一步做原生 markdown 图片尺寸缓存（`Map<uri,尺寸>` + 自定义 image rule）
- [ ] 重进会话用量同步真机验证（2026-09-09）：重进一个空闲会话，顶栏「模型 · 上下文用量」应立即出现（不再等新消息输出）；用户远端服务器（<远端服务器地址>）hermes 版本较旧，`session.usage` RPC 若不存在/结构不同则读回静默失败（顶栏保持现状，无副作用）——若远端无此 RPC，用量段仍需等首轮对话，属远端版本限制
- [ ] STT 中文识别修复验证（2026-09-08）：用户在原失败会话再录一句中文 → 输入框应出中文；服务端日志应出现 `via local whisper (small, lang=zh`（首次用 small 会先下载模型权重，首次转写偏慢属正常）
- [ ] 直连模式 Android 真机验证（2026-09-08）：本机无 Android 工具链，三端中仅剩手机端未实测——局域网直连需远端 gateway 监听 0.0.0.0（或 adb reverse tcp:9119 tcp:9119 后连 127.0.0.1）；JS 逻辑已被 web/desktop 实测与 jest 覆盖，主要看真机明文 WS/fetch 与断线重连表现
- [ ] 重进会话工具卡缺失定因（2026-09-07）：客户端已排除，待复现取证——用户用新构建在同一场景重进会话，看日志 `attach xxx：历史 N 条（… tool=z …）`：tool=0 即远端服务器（<远端服务器地址>）hermes 版本旧、投影缺 tool 行（更新远端 = 用户决策，AGENTS.md 禁改服务端）；tool>0 仍缺则回客户端渲染层继续查
- [ ] 桌面端「僵尸连接」修复验收（2026-09-07，D028）：Mac/Windows 挂机 30 分钟+ 回窗口，会话应立即可点（回前台探活 10s 内自愈）或短暂出现重连横幅后自动恢复；日志应不再出现 16~34 分钟的心跳空档（powerSaveBlocker 生效证据）
- [ ] Windows「整窗空白」取证（2026-09-07，与 Mac 僵尸连接是两条路径）：构建机开机后推包（`scripts/build-desktop-remote.sh`，已含日志埋点与本次修复）→ 复现空白 → 取回 `%APPDATA%\HermesChat\logs\main.log` 按 `docs/desktop.md` §8.6 指纹定因（候选：渲染/GPU 进程崩溃、React 异常、遮挡停绘；候选修复手段已备：render-process-gone 自愈 reload、GPU 兜底）
- [ ] 桌面端真机验收（2026-09-06）：① Mac `npm run desktop:dev` 填 SSH 配置真连（隧道/聊天/模型切换/附件/语音全流程）；② Windows 包已构建并启动验证（构建机出 `HermesChat Setup 0.1.0.exe`；win-unpacked 启动正常——回环代理 51899 持久化、渲染层已加载；ssh 会话无桌面时 GPU 降级软件渲染属正常；桌面双击 `dist-desktop/win-unpacked/HermesChat.exe` 或 Setup 安装即可用）；③ 语音 webm/opus 转写兼容性实测；④ 断线重连/HostKey 变更路径
- [ ] 桌面端交互后续包：hover 态、profile/聊天区右键菜单扩展、图片灯箱、用户消息选中/复制统一；深色模式（theme token 化后）
- [ ] 桌面端头像瞬时呈现验收（2026-09-07）：`npm run desktop:dev` 切过滤档反复横跳、三列/两列切 profile，头像应原位即时切换无渐现刷新；冷启动首进保留渐现属正常（真实加载）
- [ ] 斜杠命令补全真机/web 验证（2026-09-06）：输入 `/` 弹浮层、点选应用（含参数阶段如 `/cron ad`）、发送 `/help` 等命令看系统灰条输出、裸 `/model` 开模型面板、硬件返回键先关浮层；重点验 Android 触摸命中（浮层挂 listWrap 的假设）
- [ ] iOS 版（`docs/plan.md`：iOS 后续；SSH 隧道需 iOS 侧原生实现，见 `docs/ssh-module.md`）
- [ ] 2026-09-05 UI 打磨包真机验证（含本次语音超时修复 + 输入区图标：录音/识别中三态、超时报错、图标观感；以及会话列表三档过滤的触控观感）；头像瞬时呈现验证：切过滤档反复横跳 / 冷启动首进 Profile 列表 / 会话列表，头像应直接出现无渐现刷新
- [ ] 会话界面三连修真机验证（2026-09-05）：重进进行中会话看实时续流、kaomoji 状态条随 API 调用刷新、write_file/patch 卡片红绿 diff 与 +N −M 徽标；跨端场景（dashboard/QQ 侧续聊后 App 重进）重点验
- [ ] （可选）给 hermes 上游提 issue：`/api/audio/transcribe` 支持 language 参数（D020，上游支持后 App 侧再传语言）
