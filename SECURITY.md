# 安全策略

## 支持的版本

本项目处于 `0.x` 阶段，**只对最新版本提供安全修复**（以 [Releases](../../releases) 与 `main` 分支为准）。旧版本请先升级再报告问题。

## 报告漏洞

**请不要用公开 Issue 报告安全问题。**

请使用 GitHub 的私密漏洞报告功能：

> 仓库页 → **Security** 标签 → **Advisories** → **Report a vulnerability**

该通道只有维护者可见，我们会在此与你沟通、确认修复并协商公开时间（通常在你我商定的修复版本发布后披露，并致谢报告者——如果你愿意署名）。

如果你无法使用上述功能，也可以开一个**不含任何细节**的 Issue 说明「有安全问题需要私下联系」，我们会回复你一个私密渠道。

## 我们希望收到什么

一份有用的报告通常包含：

- 受影响的版本 / 提交号与平台（Android / Windows / macOS / 浏览器）
- 漏洞类型与影响（能读到什么、能改什么、需要什么前置条件）
- 复现步骤或最小 PoC
- 你建议的修复方向（可选）

## 在范围内的问题

- **凭据处理**：SSH 密码 / 私钥 / Session Token 的存储、传输、日志泄露、内存残留
- **SSH 隧道**：主机密钥校验绕过、中间人、转发端口越权
- **协议层**：WS JSON-RPC / REST 客户端的注入、越权调用、响应解析缺陷
- **渲染层**：Markdown 渲染的 XSS、`@image:` / `@file:` 指令导致的任意文件读取或外链泄露
- **Electron 主进程**：IPC 越权、回环代理被本机其他进程滥用、`preload` 暴露面过大
- **Android 原生模块**：`HermesSsh` / `HermesAudio` 的权限与输入校验

## 不在范围内的问题

- **Hermes Agent 服务端本身**的漏洞 —— 那是上游项目，请报告给上游；本项目只是客户端，且刻意不改动服务端
- 你自己 gateway / 主机的配置问题（例如把服务暴露到公网、使用弱口令）
- 需要设备已被 root / 已取得本地管理员权限才能利用的问题（这类前提下信任边界已不存在），但**仍欢迎告知**，我们会评估是否值得加固
- 依赖库自身的漏洞 —— 请报给对应上游；如果你愿意，也可以同时告诉我们以便尽快升级

## 已知限制（发布包使用者请留意）

- **Android 发布包目前使用 React Native 模板自带的公开 debug keystore 签名**（别名 `androiddebugkey`、口令 `android`，均为公开信息）。这意味着任何人都能签出同包名的 APK 并被系统当作合法更新接受。**在改用独立 release keystore 之前，请不要把本项目发布的 APK 当作可信来源**——建议自行从源码构建。
- **桌面端安装包未做代码签名**：Windows 首次运行会出现 SmartScreen 提示，macOS 会提示无法验证开发者。这属于未签名分发的正常现象，但也意味着无法通过系统机制校验安装包来源，请只从本仓库 Releases 下载。
- App 在设备本地以明文形式保存连接配置（含 SSH 凭据），依赖操作系统的应用沙盒隔离。设备已被解锁并取得 root / 管理员权限时，这些凭据可被读取。

## 我们的承诺

- 收到报告后 **7 天内**确认收到并给出初步判断
- 确认有效后在合理时间内发布修复，并在 Release Notes 中说明（必要时申请 CVE）
- 未经你同意不会公开你的身份
