# DECISIONS.md — 决策日志

> 记录影响架构、技术选型、设计取舍的关键决策。只记「为什么」，不复述「是什么」（实现细节在 docs/ 与代码里）。
> 维护规则（记录时机/格式/修订与归档）见 AGENTS.md「Decision Recording」段；引用 `D0XX` 编号即可定位。

---

## 活跃决策

| ID | 决策 | 不可让步 | 日期 |
|----|------|---------|------|
| D001 | React Native + TypeScript 技术栈（Android 优先，iOS 后续），对齐 web dashboard 的对话体验；本机只做 JS/TS 开发 | UI/协议层保持平台无关（桌面端复用的前提） | 2026-09-01 |
| D002 | 本机（MacBook）不装 Android/iOS 工具链；APK 构建在构建机，经 GitHub 同步（推 GitHub → 构建机拉取构建 → 取回 dist/） | Mac 上仅 tsc/lint/jest/web 静态验证；构建机的 `C:\HermesChat` 是构建副本，禁止手改 | 2026-09-01 |
| D003 | 适配官方版 hermes 公开协议（WS JSON-RPC + REST），绝不修改服务端源码/配置/数据 | 遇「不改服务端实现不了」时停下来交用户决策，禁止擅自打补丁；对 live 服务禁写（除非用户批准当次验证） | 2026-09-01 |
| D004 | 自研 SSH 隧道原生模块 HermesSsh（Android = Kotlin + JSch），否决 `@dylankenneally/react-native-ssh-sftp` | 必须有端口转发 API（该库没有）；iOS 侧不依赖其 NMSSH 实现（模拟器不支持） | 2026-09-01 |
| D005 | 原生能力差异全部收敛在 `src/ssh/`（HermesSsh/HermesAudio 契约 + 各平台实现） | UI/协议层禁止 import 平台 API（`docs/desktop.md` 桌面端方案依赖此边界） | 2026-09-01 |
| D006 | LAN 场景走明文 HTTP/WS（SSH 隧道 / 局域网直连），release 放行 cleartext | `usesCleartextTraffic=true` 必须硬编码进 AndroidManifest——manifestPlaceholders 会被 RN gradle 插件 afterEvaluate 覆盖回 false | 2026-09-01/02 |
| D007 | Android ed25519 签名打包 bcprov（bouncycastle） | JSch 的 `jce.SignatureEd25519` 需 Java15+，Android 不可用；不得换成精简依赖 | 2026-09-02 |
| D008 | 远端 hermes 启动路径运行时探测绝对路径（`command -v` → zsh/bash 登录 shell → `~/.local/bin` 等常见目录） | 不得直接用裸 `hermes` 命令——非交互 SSH 无用户 PATH | 2026-09-02 |
| D009 | zustand 选择器必须返回稳定引用 | 禁止 `?? []` 内联新对象——useSyncExternalStore 视为持续变化，Maximum update depth exceeded 闪退；缺省用模块级常量 | 2026-09-02 |
| D011 | 录音自研 HermesAudio 原生模块（Android MediaRecorder，m4a/AAC），替代 react-native-audio-recorder-player | 不引回该库：3.x 与 RN 0.87 编译不兼容，4.x Nitro 预生成代码与 nitro-modules 不匹配 | 2026-09-02 |
| D012 | Web 端调试走 `npm run web`（vite + react-native-web + 原生模块打桩 + web-only 直连入口） | 原生模块必须有 web 桩实现；web 端验证不替代真机验证 | 2026-09-02 |
| D013 | multiplex 会话归属纯客户端实现：SSH exec 只读 sqlite3 扫 session_key 命名空间建映射，foreign 会话只读、首次发送派生 `session.create(parent_session_id)` | 宿主 sqlite 只读；不改服务端（D003）；foreign 会话不写入原 profile | 2026-09-02 |
| D014 | 助手气泡 markdown 渲染：native 用 react-native-markdown-display，web 分叉 `MarkdownText.web.tsx`（markdown-it → HTML，因库主入口是未编译 JSX） | 双实现仅此一处（UI 其余单源）；库的渲染缺陷不改库源码，渲染前改写标记绕过（如列表 → 文本前缀） | 2026-09-02（09-04 修订补充列表方案） |
| D015 | 助手气泡文本选择最终方案：ChatScreen 普通窗口树内全屏选择层 + `editable` + `showSoftInputOnFocus=false` + `caretHidden` 的多行 TextInput | 不可放回 RN Modal（Android 系统选择菜单弹出即关）；不可用 readOnly（Android 完全不可选）；不可丢边缘自动滚动（只有自带滚动的多行 TextInput 做得到） | 2026-09-03 |
| D016 | 键盘避让手动测量：`useKeyboardHeight`（keyboardDidShow/Hide），按 `windowH - screenY` 计算真实遮挡区垫高 | 不用 KeyboardAvoidingView（Android edge-to-edge + adjustNothing 下不可靠）；遮挡区不得用 event.height（少报一个导航条/安全区高度） | 2026-09-03 |
| D017 | 模型切换列表自定义 provider 优先：前端 ModelPicker 按 `is_user_defined` 稳定分区提前，否决「改 hermes 配置/服务端统一顺序」路径 | hermes 无排序配置面：`model.options` 固定 `canonical_order=True`，`_reorder_canonical` 按 `CANONICAL_PROVIDERS` 硬编码声明序排内置、`custom:*` 无条件垫底，TUI/dashboard 同源同序；不改服务端（D003），组内保持服务端返回的相对顺序 | 2026-09-05 |
| D018 | 界面图标用现成 Tabler Icons PNG（240px base64 data URI 存 `src/assets/icons.ts`，`IconImage` tintColor 着色），否决手绘 View 图标与 react-native-vector-icons 图标库 | 零原生/零字体配置（数据 URI 原生与 web 通吃，tsc+web 即可完全验证，不需等构建机）；图标库要改安卓 gradle 引字体，违背 D002 的本机验证边界；新图标一律先查 Tabler（MIT）补进 icons.ts | 2026-09-05 |
| D019 | 所有 REST 调用必须带超时：`rest.ts` 统一 AbortController 30s（对齐 WS RPC 层 requestTimeoutMs），长流程外层再加 Promise.race 兜底 | RN fetch 默认永不超时——SSH 隧道半开（锁屏/切网后静默断开）时请求无限挂起，是「语音一直识别中」类卡死 UI 的根因；新 REST 端点不得裸 fetch | 2026-09-05 |
| D021 | 项目改名 HermesMobile → HermesChat（GitHub 仓库、本地目录、Android applicationId `com.hermeschat`、iOS 工程、App 显示名、文档与脚本全量替换）；构建机构建副本随之从 `C:\HermesMobile` 迁到 `C:\HermesChat` | 改名必须零残留（含包名路径）；applicationId 即应用身份，变更后手机上需全新安装、旧 App 数据不迁移（用户接受）；iOS 仅文本级改名，本机无 Xcode 不做构建验证，首次构建需重跑 `pod install` | 2026-09-05 |
| D022 | 斜杠命令补全/执行对齐 dashboard：数据源只有服务端 `complete.slash`（60ms 防抖，不做本地目录缓存）；发送侧客户端拦截走 `slash.exec`→`command.dispatch` 回退流水线（移植官方 web slashExec.ts 契约，skill/send 型转 prompt.submit）；触发仅行首 `/`（不做句中 /skill）；`/model` 不进补全、裸提交开 App 内 ModelPicker | prompt.submit 不拦截斜杠文本（不拦截就直达模型）；匹配/排序/参数提示全在服务端，本地缓存必漂移；/model 是交互式选择（TUI 同款特判），App 已有 ModelPicker 直接复用；命令输出走系统灰条（时间线 kind:system），不产生用户气泡 | 2026-09-06 |
| D023 | 桌面版布局壳分平台（修订 D001 的「UI 100% 复用」表述）：业务层（stores/RPC/聚合器/流程）与叶子组件 100% 单源，共享中间面板抽到 `src/panels/`（SessionListPanel/TimelineView/ChatInputBar/ChatOverlays/useChatComposer/sessionFlows），布局壳 `src/desktop/` 仅 web 构建使用；布局按**窗口宽度**驱动断点（<900 单列 / 900–1279 两栏 / ≥1280 三栏），不用 `window.hermesDesktop` 区分布局——浏览器拉宽即可调试桌面布局，手机构建零影响 | 手机屏幕行为保持（ChatScreen/SessionListScreen 改薄壳为等价重构）；桌面选中态进 desktopUiStore，不与 react-navigation 混用 | 2026-09-06 |
| D024 | 桌面渲染层从主进程回环服务加载（`http://127.0.0.1:<port>`）而非 file://：同源无 CORS，`/api`（HTTP+WS）由主进程反代到隧道端口并重写 Host/Origin（复用 vite dev proxy 已验证策略）；监听端口跨次启动持久化复用 | 端口变 = localStorage origin 变 = SSH 配置全丢，端口必须记忆复用；渲染层禁止直连隧道端口（Origin 防护会拦 WS） | 2026-09-06 |
| D025 | 桌面平台能力走「主进程桥 + 渲染层 canvas」组合：文件选择/读字节 = IPC（dialog+fs）产 data URL；图片压缩/头像裁剪 = 渲染层 canvas（`utils/media.ts` web 分支，不引原生图片库）；语音 = getUserMedia+MediaRecorder（mp4/aac 优先）；桥接口在 preload contextBridge 暴露，`vite alias './HermesSsh' → desktopHermesSsh` 使 SshTunnelTransport/SshManager 单源复用（不复制 bootstrap 逻辑） | 渲染层无 Node 能力（contextIsolation）；不引需要原生编译的新依赖；手机附件链路（documents-picker）保持默认，桌面经 ChatInputBar 的 pickers 注入 | 2026-09-06 |
| D026 | web 端右键菜单（桌面会话行）用行组件 ref 拿 RNW 透传的宿主 div 直接 `addEventListener('contextmenu')`，否决 React `onContextMenu` prop 路线 | 实测本项目 RNW 环境下 React 委托对 contextmenu 不派发（#root 委托监听在、fiber props 在、真实/合成右键均无响应，合成 click 却正常）——后续任何人不得「简化」回 onContextMenu prop；原生端 ref 无 addEventListener 能力自然跳过，RN 原生行为不受影响 | 2026-09-07 |
| D027 | 桌面/web 头像缓存 = 渲染层内存 blob: object URL（`avatarCache` web 分支：data URL→Blob→createObjectURL，按 profile 名进程内记忆化，换图先 delete 再重建），否决 Electron 主进程落盘 + 自定义协议方案 | 移动端 b390293 的修复哲学「确定性缓存路径跳过渐现」在 web 的等价物是**会话期内稳定的 URI**——blob: URL 稳定、浏览器按 URL 命中内存缓存、重挂载零解码；落盘方案只为冷启动秒显，需新增主进程桥 API 与协议注册，与本闪烁 bug 无关，不为此扩桥面；冷启动首拉保留渐现属真实加载，可接受 | 2026-09-07 |
| D028 | 桌面端常驻策略：`powerSaveBlocker('prevent-app-suspension')` + `backgroundThrottling: false` + 应用层探活（回前台必探 + ready 态 30s 周期探 `/api/health`，连续 2 次失败判死触发重连），治「macOS App Nap 冻结全进程 → 远端掐断连接 → 回前台僵尸窗口（state=ready 假活、点击无响应、无断线横幅）」（2026-09-07 Mac 日志实锤：心跳间隔被拉到 16~34 分钟） | 常驻聊天客户端（后台要发回复通知）明确不参与系统挂起，接受挂机期额外耗电；探活只复用已有 `/api/health` 端点，不要求服务端加 WS ping 帧（D003）；探活仅 ready 态运行，reconnecting 期间交给退避重连（重连本身就是探测） | 2026-09-07 |
| D029 | 修订 D010：以「配置类型字段 + Transport 分发」重新引入直连模式（`ConnectionProfile.type: 'ssh'\|'direct'`，三端工厂按 type 分发：原生 DirectTransport / 桌面 DesktopDirectTransport（主进程代理上游=gateway + 代取 token）/ web WebDirectTransport），旧 v2 数据缺 type 迁移为 ssh | 直连只连**已运行**的 gateway，不做进程拉起（那是 SSH bootstrap 的职责，桌面 spawn 属于扩权不做）；平台差异必须收敛在 Transport 层——桌面渲染层与 gateway 跨源（Host/Origin 防护 + CORS），直连的 token 提取与 /api 流量必须走主进程（`desktop:directConnect` IPC），与 D024 的回环代理策略同源；浏览器端直连固定经 vite 代理（目标本机 9119），不做通用跨源直连 | 2026-09-08 |
| D030 | 助手消息复制 Markdown 源码的分层策略——桌面/web：拖选渲染文本 + Cmd/Ctrl+C 经 document 级 copy 事件拦截替换剪贴板（markdown-it token.map 注入 `data-md-map` 做**块级**源行映射，跨消息选择不干预），辅以气泡右键菜单「复制 Markdown / 复制纯文本」；手机：保留长按弹原始文本层（TextInput 系统选择复制），加「复制全部」按钮（`@react-native-clipboard/clipboard`，`clipboard.ts`/`clipboard.native.ts` 平台变体防 vite 打包原生模块） | 块级粒度是源码映射的精度上限（行内选择无法无损映射回带符号源码，宁多勿缺、结构不破碎）；移动端否决 QQ 式渲染文本自由拖选——RN `Text selectable` 跨块碎裂、自绘选择控件（手柄/放大镜/手势协调）成本与个人项目不匹配，长按弹层已是 RN 生态最优解；copy 拦截必须 document 单例监听 + 容器注册表（copy 事件派发到 activeElement 不冒泡经气泡） | 2026-09-08 |
| D031 | mid-turn 重进（App 重启）丢结构块的修复 = inflight 纯文本重建的流式尾部打 `fromInflightProjection` 标记 + `message.complete` 时用 `session.history`（live sid）重拉历史整体 hydrate（turn 落盘后投影含 tool 行/reasoning，结构全恢复）；**否决** `session.events.since` 事件重放整轮重建路线 | 不改服务端（D003）；replay 环每会话仅 512 帧且 message.delta 逐帧计数，长 turn 必然 truncated，整轮结构无法从事件流补全——只有落盘历史是完整结构源；重拉结果仅在聚合器非流式时应用（新 turn 已开始则让位，避免清掉流式尾部）；同进程 WS 重连（previousStreaming 前缀复用）不打标不重拉 | 2026-09-08 |
| D032 | 会话标题即时刷新 = 「命令后主动读回 + 事件回填」：`/title` 命令执行后调 `session.title` **只读形式**（不带 title 参数）读回权威标题；`session.title`/`session.info` 事件也回填；落地两处（chat store 顶栏标题 + sessions store 列表行 `patchTitle` 就地替换），顶栏组件改为订阅 chat store，路由参数/壳选中态只作「打开那一刻」的种子 | 手动 `/title` 经 slash worker 写库且服务端不推任何事件（D003 不改服务端），客户端等不到事件；`sessions.changed` 只置脏、进列表才拉，且刷新会重建 nsMap（SSH sqlite 扫描），不能每次广播都拉列表；读回用只读 RPC 而非客户端自己调改名 RPC——显示值必与服务端 sanitize 后一致，命令失败（重名等）时读回旧标题不产生假更新；空标题不回填（服务端未落库时不得抹掉已有标题） | 2026-09-08 |
| D033 | 会话列表两档排序（「最近消息」默认 / 「创建时间」）：最近消息档 = **保持服务端返回序**（`session.list` 写死 `order_by_last_active=True`，投影虽无 last_active 值但顺序即权威，压缩链感知），multiplex 跨库合并（有 foreign 行）时用 namespaceMap exec 扫描补齐每行活跃时间（`MAX(last_activity_at, MAX(messages.timestamp))` 兜底 started_at，同服务端 `_sql_session_last_active` 口径）稳定排序交错；创建时间档 = 纯客户端按 started_at 排；档位持久化 AsyncStorage `hermes.sessionSort.v1`（App 启动时恢复） | 无 foreign 行时不得用扫描值重排单库列表（服务端序 > 行级近似）；活跃时间不走 REST `/api/sessions`（行序与 WS 同源，徒增认证/超时路径）；扫描从 `agent:%` 过滤放宽为全表是成本上限（`idx_messages_session` 索引覆盖，336 行库实测 35ms）；否决「客户端自己按 preview/消息数估算活跃度」类启发式 | 2026-09-08 |
| D034 | 修订 D020：STT「中文识别成英文」真正根因 = 转写按**会话所属 profile** 读各自 config.yaml（`/api/audio/transcribe` 的 `?profile=` → `_config_profile_scope` 切 HERMES_HOME），main/default 已改 zh 而其余 4 个 profile 缺 `stt.language` → 自动检测被 base 误判 en；修复 = 经用户授权把本机 6 个 profile 全量配 `stt.language: zh` + `local.model: small`（服务端配置修改，App 零改动）；否决前端本地 STT（Electron Web Speech 无 Google key 不可用、Android 需新原生模块）与免费云 STT（API key 管理、语音出本机、配额，违背自托管架构；云识别走服务端 stt provider） | 只动 yaml 不动服务端源码（D003）；STT 语言/模型是每 profile 独立配置，**新增 profile 必须同步带 `stt.language`**，否则该 profile 语音退回自动检测误判 | 2026-09-08 |
| D035 | 聊天流式滚动跟随显式化（修订「inverted 隐式行为足够」的默认认知）：TimelineView 持 `followRef`（距底 ≤32px=在底部，onScroll 同步更新、切会话重置），内容增长时跟随态显式 `scrollToOffset(0)` 钉底、非跟随态做**锚定补偿**（原生直接 scrollTo(offset+delta)；web 下一帧经 `getScrollableNode()` 读真实 scrollTop、仅未被移动过才补）——否则流式增长把上滑用户的视口往新消息方向拖（Android RN ScrollView 无可见位置锚定）；发送消息（useChatComposer `onAfterSend` → `revealBottom` 句柄）立即回底恢复跟随；busy 不参与判定（在不在底部即语义），历史 hydrate/图片加载等非流式增长同规则 | 用户上滑阅读是最高优先级——任何内容增长不得移动其视口；补偿必须以「视口未被移动过」为前提（用户手势或平台 anchoring 已调整则跳过，否则双重补偿/覆盖手势）；web 端禁用 onScroll 节流状态判断 anchoring（32ms 节流会竞态），必须下一帧读真实节点 | 2026-09-09 |
| D036 | 头像「深色圆角线框」修复：像素级定因实锤根因 = `Avatar` 给 Image 垫的 avatarColor 底色从圆角裁剪的抗锯齿边缘渗出（白底头像四周一圈彩色线框；RGB 色度环分析定位，灰度统计/小尺寸目视均会漏），外层 View+内层 Image 双层同半径 borderRadius 为同类加重因素；修复 = 圆角只留一层（Image 自身）且**图片分支彻底不垫底色**，三端无需重传头像即生效（2026-09-10 桌面双端验证通过）；否决上传管线内缩裁边（文件无暗带可裁）；顺带引入 patch-package 给 ImageResizer 打 JPEG 白底展平补丁（Android/iOS），根治「透明 PNG 转 JPEG 烤黑边」隐患（预乘 alpha 透明处 RGB=黑，本次未触发） | 圆角在 RN 里只允许一层，且**圆角元素下方不得垫彩色底**——抗锯齿边缘必然混入底色（真实 GPU 窗口可见、无头截图/灰度分析会漏，取证必须真实窗口 + RGB 色度）；patch-package 是首次引入的原生补丁通道（`postinstall` 自动应用，npm ci 兼容），只用于第三方库的确定性行为修复，不承载业务逻辑 | 2026-09-09（09-10 验证收口） |
| D037 | 修订 D035：锚定补偿前提收窄为「**流式增长 + 用户停滚**」（`shouldRestoreAnchor(follow, deltaH, streaming, !scrolling)`）；非流式增长（历史 cell 懒挂载/图片加载，发生在视觉顶端一侧、本不移动视口）一律不做程序性滚动——否则补偿把用户向前瞬移并打断惯性滚动，正是「首次上滚抖动、浏览一遍后恢复平滑」的根因（原生分支 offsetUntouched 为同值自比较恒真＝每次增长必跳）；「流式中上滑滚入未挂载区」两类增长并存不可分辨，靠「距上次 scroll 事件 <200ms 视为滚动中则不补」让位给手势与平台 anchoring，停滚后的流式增长才补。配套首滚减负：web `<style>` 模块级单次注入（原每气泡一份，长会话批量挂载反复触发全文档样式重算）、md.render 结果 FIFO 缓存（300 条，卸载重挂载零解析）、FlatList `maxToRenderPerBatch=6` | 用户上滑阅读最高优先级不变（D035，但「任何内容增长不得移动视口」修正为「任何**非手势**滚动不得移动视口」）；已挂载 cell 在 windowSize（默认 21 视口）内不卸载是「浏览过一次后平滑」的根基，不得为省内存调小；流式结束后的 history 重建（D031）不补偿（停在历史位置可能一次位移，可接受）；原生 markdown 图片尺寸缓存留待 D035 真机复测时一并评估 | 2026-09-09 |
| D038 | 定时任务管理走 dashboard REST `/api/cron/*`（与 `/api/ws` 同端口同进程、复用现有 `X-Hermes-Session-Token`），否决 WS `cron.manage` RPC 为主通道——后者只有 list/add/remove/pause/resume，**无 update/trigger/runs**；也否决 gateway 8642 端口 `/api/jobs`（需独立 API_SERVER_KEY 第二凭据、PATCH 字段白名单窄）。列表自动刷新订阅 WS `cron.changed`（服务端每秒轮询 jobs.json mtime 广播，payload 空）；`cron.changed` 接线放 cron store 内按 RPC 实例自接线而非 connection.ts wireEvents——避免 connection↔cron 循环导入（cron 的 REST 参数取自 connection store）；trigger 立即运行为同步长操作，客户端单独放宽超时至 10 分钟（dashboard 桌面客户端给 24h），409=已在运行 | 不引第二凭据（API_SERVER_KEY）；token 复用现有会话 token；cron.changed 仅在本会话加载过列表后响应（没开过定时任务视图不白发请求） | 2026-09-10 |
| D039 | Android「markdown 表格横向拖不动（偶尔点击后能拖）」根因 = 竖向 FlatList 的原生触摸拦截先于子级判定，垂直位移过 touchSlop 即抢走整个手势（横向拖动带早期垂直抖动是常态，点击后重新起手路径更平则偶尔成功）；修复 = 首次引入 **react-native-gesture-handler（3.2.1，v3 hook API：`GestureHandlerRootView` 挂原生入口 index.js 根节点 + `usePanGesture` 方向锁定驱动表格 ScrollView，`activeOffsetX ±10 / failOffsetY ±8`，Android 专属，iOS/web 路径不变）**。否决「加 `nestedScrollEnabled`」——RN 0.87 JS 层已默认 true（ScrollView.js `?? true`）且该机制只对同轴嵌套生效（RN 源码只修了横套横：ReactHorizontalScrollView 的 findDeepestScrollViewForMotionEvent，竖套横无保护）；否决自制 PanResponder——JS responder 声明抢不过原生 onInterceptTouchEvent（事件先过原生拦截链才到 JS），只有 RNGH 的根节点 dispatchTouchEvent 编排能赢；否决 patch RN——ReactAndroid 是预编译产物，patch-package 不适用 | RNGH 只进原生 bundle（web 入口不引用，web:build 已验证产物零 RNGH 代码）；Android 表格 `scrollEnabled=false` 完全由 Pan 驱动（无双驱动竞态），惯性用 JS 速度衰减补偿；Pan 未激活不拦任何事件，气泡长按复制/点击不受影响；iOS 首次构建时需 `pod install`（D021 先例注记） | 2026-09-10 |
| D040 | 修订 D038 交互：定时任务**点条目默认打开编辑**（原=运行历史）；**点运行记录打开「运行详情」只读回放**（元信息 + 对话内容），不再 `session.resume` 进聊天页——resume 会挂起宿主 profile 的 agent，浏览历史不能有副作用；也不走 SSH sqlite（`fetchRemoteHistory`，direct 连接无 exec 通道是缺口）。数据链 = dashboard REST `GET /api/sessions/{id}/messages`（与 `/api/cron/*` 同端口同 token，服务端 read_only 开库 + 压缩链解析，详见 docs/protocol.md §7）；resume 降级为详情页「在聊天中打开」的显式动作。运行历史/详情弹层白底与 header 一致 + 720 居中；编辑表单提示词区加高（原生 minHeight 160 自动撑高；web 固定高 200 + PanResponder 拖拽手柄 120–480，高度应用运行期内记忆） | 只读浏览零副作用（不 resume、不写库）；不引第二凭据；详情页不渲染工具调用卡（该端点返回原始行、无 gateway 侧工具投影，与 foreign 只读视图同口径）；桌面 cron 视图下 openChat 必须先 selectProfile（聊天区只在选中 profile 分支渲染） | 2026-09-10 |
| D041 | 修订 D040 ⑤：定时任务提示词输入区**删除桌面拖拽调高**（用户否决），三端统一「默认 ≈8 行随内容自动撑高 + 上限 25 行」（显式 lineHeight 20：min 160 / max 520，超出内部滚动）；web 端 textarea 无原生长高，沿用 ChatInputBar 先例在文本变化时手动量 scrollHeight 设高（先收回最小高再量，否则删行后缩不回） | 编辑窗口视觉上限 = 25 行文本（用户指定）；不做手动调整高度；原生靠样式 minHeight/maxHeight 约束，web 靠量高 effect，两端观感一致；「N 行」的像素换算必须含上下 padding（20），且要显式 lineHeight（平台默认行高不同，上限会漂） | 2026-09-11 |
| D042 | 对服务端 wire 形态的**宽容归一化**（2026-09-11「clarify 挂起时打开会话失败 iterator method is not callable」修复）：`session.resume` 的 `pending_approval`/`pending_clarify` 是**单个对象**（`dict \| None`，无挂起时字段不存在），客户端曾按数组声明直接 `for...of` → Hermes 抛 TypeError、弹「打开会话失败」；修复 = 类型改 `OneOrMany<T> = T \| T[]`（兼容旧服务端列表形态）+ `store/chat.ts` 新增 `asPendingList()` 在 `applyPending` **单点归一化**，四个入口（openSessionFlow/fork/SshManager/attach）共用；顺带把批量快照的 `answers` 播种为「已作答」（对齐官方 desktop 的 lockedAnswers）。同类问题定位法：报错文案先按 JS 引擎内建串分叉（该串是 `libhermesvm.so` 内建，源码 grep 零命中） | 不按服务端字段的「想当然类型」迭代——挂起交互一律 `dict \| None`；wire 字段的形态归一化收敛在 store 边界单点，新调用点一律经 `attach`/`reattachAfterResume`，禁止在 flow/UI 层直接迭代 resume 字段；不为了兼容旧形态而放弃当前形态的严格断言（测试同时覆盖 dict 与 list） | 2026-09-11 |
| D043 | 开源前敏感信息治理：本机/个人专属值**一律走环境变量或 gitignored 配置文件**，不入库。落地三条——①构建机主机名/工程目录/Git Bash 路径/adb 路径/设备序列号收敛到 `scripts/build-env.sh`（gitignore，模板 `scripts/build-env.example.sh` 入库），`scripts/*.sh` 启动时自动 source、内置通用默认值兜底；②文档里的绝对家目录一律写 `~`、公网地址写 `<远端服务器地址>`、序列号写 `<设备序列号>`；③开发截图 `docs/screenshots/`（冒烟脚本产物，含真实会话标题与聊天正文）整个 gitignore，只留本地。历史中已存在的同类内容用 `git filter-repo --path docs/screenshots --invert-paths --replace-text` 一并重写（否则删文件只是新增一次提交，旧 blob 仍可检出） | 真实主机名、SSH 别名、内网/公网 IP、家目录绝对路径、设备序列号、真实会话数据、个人邮箱一律不得入库；本机专属值只在 `scripts/build-env.sh` 里出现；配图必须用 `scripts/mock-gateway.js` 的假数据重拍；新增文档/测试夹具的示例值一律用 `example.com`/`192.168.1.10`/`buildhost` 这类明显占位符；提交前按 AGENTS.md「敏感信息纪律」自查 | 2026-09-11 |
| D044 | 许可证选 **PolyForm Noncommercial License 1.0.0**（源码公开 + 非商业免费 + 商业使用需单独授权），而非 MIT/Apache 等宽松许可，也非 AGPL 双许可。理由：用户要求「可完全免费自由使用，但商业收费需许可」，PolyForm NC 的「Noncommercial Purposes / Personal Uses / Noncommercial Organizations」三段正好逐条对应（个人与研究免费、慈善/教育/政府免费、商业需授权），且由专业律师起草、条款现代（含专利授权与专利报复条款）。依赖侧无冲突：43 个 MIT + 2 个 Apache-2.0，全为宽松许可，不产生 copyleft 冲突 | 必须明确本项目不是 OSI 开源许可，对外表述用「源码公开（source-available）/ 非商业免费」，不得称「开源」以免误导；LICENSE 顶部的 `Required Notice:` 版权行是 PolyForm 条款要求的通知载体，改动版权主体时必须同步；**因商业授权需要再许可权，接受外部贡献前必须要求贡献者签 CLA（DCO 不足以支撑商业双许可）**；若日后改为 OSI 开源，需全员贡献者同意或重写其贡献 | 2026-09-11 |
| D045 | **修订 D044**：许可证从 PolyForm Noncommercial 1.0.0 改为 **AGPL-3.0-or-later**（用户决定：允许他人商用，条件是衍生作品开源）。理由与影响：①AGPL 是 OSI 认可的自由软件许可，「开源」表述成立，不再有 F-Droid 收录障碍与「非商业许可被公司政策禁用」的问题；②第 13 条「Remote Network Interaction」覆盖本项目特有的**浏览器/网络服务形态**——GPL 只管「分发」，管不到「只在服务器上跑不分发」，AGPL 补上这个口子；③**放弃按商业授权收费的能力**：AGPL 下任何商业使用只要遵守开源义务即免费，不再存在「商业需单独许可」；若日后重启商业双许可，必须先建立 CLA 并追认既有贡献（这也是本轮同步把 README 贡献条款定为 DCO 的原因——DCO 足以支撑纯 AGPL，但不足以支撑双许可）。落地：LICENSE 换成 FSF 官方 agpl-3.0.txt **逐字原文**（GPL 系许可要求 verbatim，禁止增删，故版权声明不放 LICENSE 而放 README 与源码头）；package.json 加 `"license": "AGPL-3.0-or-later"` | LICENSE 必须是 FSF 原文，任何情况下不得改一个字（与 D044 的 PolyForm 不同，后者允许/要求附 Required Notice 行）；许可证表述统一用 `AGPL-3.0-or-later`（SPDX 标识），源码头尽量补 SPDX 行；贡献走 DCO（`Signed-off-by:`）而非 CLA；依赖侧已核实全为 MIT/Apache-2.0，与 AGPL 兼容 | 2026-09-11 |
| D046 | 开源文档语言策略：**`README.md` 用英文作为默认展示**，中文版独立为 `README.zh-CN.md`，两者顶部互相语言切换链接；同时把「界面多语言（i18n）」正式列入路线图（`docs/plan.md` M6）。理由：GitHub 是国际流量入口，英文 README 决定项目能否被非中文用户检索与理解；而设计与踩坑文档（`docs/`、`PROGRESS.md`、`DECISIONS.md`）保留中文——它们是维护者自用的开发资产，翻译成本高且更新频繁，强行双语会迅速腐化。**界面 i18n 与文档双语是两件事**：本轮只做文档，App 内文案仍是硬编码中文 | 两份 README 的章节结构必须保持一一对应（同增同删，改一版必须同步另一版，否则语言切换等于欺骗用户）；示例值一律用占位符（`example.com`/`192.168.1.10`），英文版不得引入任何本机信息；README 里承诺的 i18n 必须标注为「计划中」而非已支持，避免虚假宣传；`docs/` 暂不翻译，但英文 README 需明确标注设计文档为中文，避免读者以为文档缺失 | 2026-09-11 |
| D047 | 界面 i18n 落地（实现 D046 列入的 M6）：**自研轻量框架**（`src/i18n/` 词典 + `t()` 取用层），不引 i18next/react-intl——延续 D004/D011/D018 的少依赖哲学，且必须能被 tsc + jest + web 冒烟闭环验证（D002）；**每语言单文件词典**（`en.ts` 为键权威来源 `as const`，`zh-CN.ts` 用 `Record<MessageKey, string>` 类型强制对齐，漏键即编译错误），否决「并排单表」（加第三种语言要改每个条目）与「按域拆分」（文件多、跨域查键难）；语言策略 = 跟随系统（BCP47 归一，zh* 归 zh-CN，**不支持的语言回退 en**）+ `hermes.locale.v1` 持久化手动覆盖（LanguageToggle 三态循环，暂不做完整设置页）；新增文案翻译 = AI 协作翻译 + tsc 编译期强制 + `scripts/i18n-check.js` 扫描硬编码 CJK（入 CI），否决机器翻译 API 脚本（引外部依赖、译文质量不可控）；desktop 主进程为独立翻译域（`desktop/i18n.ts` + `dt()`）——desktop tsconfig 隔离编译（rootDir 限定）不能引用 src/i18n，且文案仅十余条 | 键必须先登记 `en.ts` 再用（键即契约）；存量约 70 文件 400+ 条文案已全量迁移，此后任何用户可见文案禁止硬编码（i18n-check 拦截）；已持久化字符串（聊天历史错误条、已存配置名）不回溯翻译；诊断日志（dlog/appendLog/console）与 dev 工具（scripts/、web-stubs）保留中文不翻译 | 2026-09-13 |
| D048 | web/桌面端时间线内可点元素在流式移动内容下必须用 onPressIn（2026-09-15「agent 输出时无法展开推理/工具卡」修复）：RNW PressResponder 的 onPress 走 **DOM click 事件**（"Only when the browser produces a click event is onPress invoked"），浏览器要求 mousedown 与 mouseup 落在同一元素才派发；流式钉底期间内容增长把卡片不断顶走，按下→抬起之间 click 永不触发（Playwright 实证真实鼠标 6/6 失败，DOM 级派发点击则正常且展开态跨 delta 保持——先排除 remount/状态重置方向再动按压层）。修复 = `src/utils/expandPress.ts` 按压 props 工厂：web 端 onPressIn（responder grant = mousedown 落点瞬间）切换 + onPress 仅放行键盘/程序化 click（UIEvent.detail 恒 0，鼠标 ≥1）防双触发；原生 RN Pressability touchdown 锁定 responder、release 无条件触发 onPress，维持原路径不动。否决「指针按下期间锚定补偿冻结内容」（动 D035/D037 两次缺陷史的滚动状态机，同收益高风险，留作「流式中选中文本」的储备方案）与「全平台 onPressIn」（移动端卡片标题起手势滚屏会误展开） | 时间线/列表内新增可点元素时，web 端按压一律经 expandToggleProps（或有同等理由），禁止裸 onPress；`detail=0` 兜底是键盘无障碍唯一入口，删除前必须有替代；此类修复的边界 = 原生路径零改动；诊断「点不动」先按「静态是否正常 / DOM 级触发是否正常」两刀分叉，区分按压契约与状态/remount 问题 | 2026-09-15 |
